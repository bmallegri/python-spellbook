// All the game content. No React in here.

// Three states, one metaphor, and the same three words everywhere in the game:
// the ink is smudged (you can't read it yet), forming (open, not set), or inscribed.
export const SMUDGED_INK = "#5f6570";     // slate
export const FORMING_INK = "#7a5d22";     // brass
export const INSCRIBED_INK = "#2c6273";   // tide

export function stateInk(state) {
  if (state === "inscribed") return INSCRIBED_INK;
  if (state === "locked") return SMUDGED_INK;
  return FORMING_INK;
}

export const INK = {
  ink: "#e7e3d9", ink2: "#dbd5c8", page: "#f1eee6", pageHi: "#f1eee6",
  line: "rgba(34,38,43,0.14)", lineHi: "rgba(34,38,43,0.26)",
  text: "#22262b", dim: "#4c5158", faint: "#5f6570",
  onAccent: "#f1eee6",
  vellum: "#ded9cb", vellumInk: "#22262b", vellumEdge: "#c7c0b0",
  brass: "#7a5d22",
  // older names, still what most of the interface calls these
  candle: "#7a5d22", verdigris: "#2c6273", oxblood: "#8a3f2f", terreVerte: "#4a6146",
};

// The four inks a real manuscript would have had. Every school takes one, so
// "standing by school" reads at a glance instead of coming out one flat blue.
export const SCHOOL_INK = {
  tide: INK.verdigris,      // #2c6273
  verte: INK.terreVerte,    // #4a6146
  oxblood: INK.oxblood,     // #8a3f2f
  folium: "#6a4a6e",        // turnsole, the fourth manuscript ink
};

export const SCHOOL = {
  inscription: { name: "Inscription", color: SCHOOL_INK.tide, blurb: "The first inks. Everything else is built on these." },
  transmutation: { name: "Transmutation", color: SCHOOL_INK.verte, blurb: "Reshaping what is already written." },
  divination: { name: "Revelation", color: SCHOOL_INK.oxblood, blurb: "Making a table say out loud what it has only been hinting at." },
  artifice: { name: "Conjuration", color: SCHOOL_INK.folium, blurb: "Things that keep working after you walk away." },
};

// say = read it aloud, image = what to picture, when = when to reach for it
export const SPELL = [
  { id: "varibuddy", name: "Variables", school: "inscription", rank: 1,
    real: "Variables, types & f-strings",
    desc: "A vessel that holds a value and hands it to any later spell. int / float / str / bool, and f-string weaving.",
    say: "Let 'greeting' hold the woven phrase “Hi, {name}”.",
    image: "Write on the lid before you fill the jar. Always before. A jar you fill first and name later is one you open in six months without recognizing it. An f-string is a line of text with gaps cut into it, and the braces are the gaps.",
    when: "You need to store something and reuse or show it later.",
    code: 'hero = "Luna"\nlevel = 7\nprint(f"{hero} reached level {level}")',
    runes: ["greeting", "=", 'f"Hi {name}"'],
    mission: "Store your name and a number, then print them woven into one f-string.",
    gotcha: "b = a copies the label, not the value. For lists and dicts, both names point at the same object, so changing one changes the other. And f-strings need the f: f'{name}' works, '{name}' just prints the braces." },
  { id: "condifork", name: "Control Flow", school: "inscription", rank: 1,
    real: "Control flow (if / for / while)",
    desc: "Chooses and repeats. if / elif / else to decide, for and while to loop, break and continue to steer.",
    say: "For each t in laps: if t beats best, keep t.",
    image: "A signpost that only shows true writing while you're standing under it. if reads the post, then picks. for never picks at all. It walks the same road once for every name on the list and stops when the names run out.",
    when: "You must choose between paths, or repeat an action over many items.",
    code: "best = 999\nfor s in scores:\n    if s < best:\n        best = s",
    runes: ["for", "t", "in", "laps", ":"],
    mission: "Loop over five numbers and print only the ones above ten.",
    gotcha: "for...else runs the else only when the loop finished without a break. Surprising the first time. Comparison is ==, while = is assignment; if x = 5 is an error." },
  { id: "listling", name: "Lists and Dicts", school: "inscription", rank: 1,
    real: "Data structures (list / dict / set / tuple)",
    desc: "Four vessels for many things: ordered list, keyed dict, unique set, sealed tuple. Choosing well is half the craft.",
    say: "Let 'laps' hold a row of two times.",
    image: "A shelf where the jars keep the order you set them in. A cabinet where every drawer wears a written label and no two labels match. A bag that quietly swallows the second copy of anything. And one box nailed shut. Choose by how you'll reach in, not by what you're putting away.",
    when: "You're holding more than one thing and want them together.",
    code: 'prices = {"tea": 3.5, "cocoa": 4.0}\ncheapest = min(prices, key=prices.get)',
    runes: ["laps", "=", "[78.2, 78.4]"],
    mission: "Build a dict mapping three drinks to their prices, then print the cheapest drink's name.",
    gotcha: "{} makes an empty dict, not a set. Use set() for that. Lists keep order and allow duplicates, sets drop both, and dict keys must be unique." },
  { id: "comprehendra", name: "Comprehensions", school: "inscription", rank: 2, from: "listling",
    real: "Comprehensions",
    desc: "Weaves a whole new collection from an old one in a single luminous line. The mark of a fluent hand.",
    say: "Gather x-squared, for each x drawn from nums.",
    image: "A loom that spins a whole new cloth in one pass. You speak the pattern once and every thread arranges itself.",
    when: "You'd otherwise write a loop just to build a new list or dict from an old one.",
    code: "squares = [x**2 for x in range(10) if x % 2 == 0]",
    runes: ["[", "x**2", "for", "x", "in", "nums", "]"],
    mission: "Rewrite yesterday's for-loop as a single comprehension.",
    gotcha: "A comprehension builds a whole new list in memory at once. For huge data a generator (x for x in ...) is far lighter. Nesting more than one for gets unreadable fast." },
  { id: "functo", name: "Functions", school: "inscription", rank: 2,
    real: "Functions & scope",
    desc: "Binds logic you reuse into one named sigil. Arguments, defaults, *args/**kwargs, return, and local vs global scope.",
    say: "Define 'delta', taking a and b.",
    image: "Someone cut the working into the handle of a paring knife so they would never have to reason it through twice. That's a function. Think it once, cut a name into it, and afterwards only say the name. What you put in the handle comes back off the blade.",
    when: "You catch yourself repeating the same few lines in different places.",
    code: "def delta(a, b, *, unit='s'):\n    return f'{a - b:.3f}{unit}'",
    runes: ["def", "delta", "(a, b)", ":"],
    mission: "Write a function that takes two scores and returns the gap as text.",
    gotcha: "Never use a mutable default like def f(items=[]). That list is made once and shared across every call. Use items=None, then items = items or [] inside." },
  { id: "trycatchu", name: "Exceptions", school: "inscription", rank: 1,
    real: "Errors & exceptions",
    desc: "Catches a failure before it shatters your script. try / except / else / finally, raising, and custom wards.",
    say: "Try to make an int; if it can't (ValueError), fall back to 0.",
    image: "You ask the kettle for a number and the kettle hands you a wet leaf. try is asking anyway. except ValueError is the sentence beginning \"and if it's a leaf, then\". You still have no number. You have a plan for having no number.",
    when: "A line might fail on bad input and you don't want everything to crash.",
    code: "try:\n    val = int(user_input)\nexcept ValueError:\n    val = 0",
    runes: ["try", ":", "except", "ValueError", ":"],
    mission: "Wrap an int() call so bad input yields 0 instead of crashing.",
    gotcha: "A bare except catches everything, even Ctrl-C and your own typos. Always name the type, like except ValueError. Catching an error is not the same as fixing it." },

  { id: "mirrorrite", name: "Copying", school: "inscription", rank: 2, from: "listling",
    real: "Shallow vs. deep copy",
    desc: "Copying a collection safely. A shallow copy shares the inner objects; a deep copy clones everything all the way down. Mixing them up is a classic hidden bug.",
    say: "Mirror the shelf. But do the reflections share the very same jars?",
    image: "Copy the shelf and you get a second shelf holding the very same jars. Stir one in the copy and the original is stirred, because there was only ever one jar. copy.deepcopy blows fresh glass for every last one of them.",
    when: "You copy a list or dict that holds other lists/dicts and don't want edits to leak between the original and the copy.",
    code: 'import copy\na = [[1, 2], [3, 4]]\nshallow = a.copy()        # inner lists shared\ndeep = copy.deepcopy(a)   # fully independent\nshallow[0].append(99)     # this also changes a!',
    runes: ["import copy", "copy.deepcopy", "(", "a", ")"],
    mission: "Make a list of lists, shallow-copy it, change an inner list, and watch the original change too. Then fix it with deepcopy.",
    gotcha: "list.copy() and [:] only copy the top level. Nested lists and dicts are still shared. Reach for copy.deepcopy() the moment your data has layers." },
  { id: "nestedra", name: "Nested Data", school: "inscription", rank: 2, from: "listling",
    real: "Nested collections",
    desc: "Collections inside collections. A list of dicts, a dict of lists. Real data is shaped this way, and you reach in with chained [ ] keys and indexes.",
    say: "A reliquary of reliquaries. Reach the shelf, then the jar, then the value.",
    image: "orders[0]['sales'] is two motions, not one. The first bracket picks a crate off the stack. The second opens a labelled drawer inside that crate. Each pair of brackets is one more hand going in.",
    when: "Your data is a list of records, or a dict whose values are lists/dicts. API responses and CSV rows almost always are.",
    code: 'shop = {"drinks": [\n    {"name": "chai", "sales": 30},\n    {"name": "matcha", "sales": 25},\n]}\nprint(shop["drinks"][0]["name"])   # chai\ntotal = sum(d["sales"] for d in shop["drinks"])',
    runes: ["shop", '["drinks"]', "[0]", '["name"]'],
    mission: "Build a list of at least two dictionaries, then print one value reached through both an index and a key.",
    gotcha: "Reaching deep blindly, like data[0]['x'][2], raises an error if any layer is missing. Use .get() for dicts and check lengths before indexing." },
  { id: "argstar", name: "args and kwargs", school: "inscription", rank: 2, from: "functo",
    real: "*args and **kwargs",
    desc: "Functions that accept however many arguments you throw at them. *args gathers extra positional values into a tuple; **kwargs gathers named ones into a dict.",
    say: "Open the basket. Catch whatever they bring, however much.",
    image: "A basket left open on the counter at closing. Whatever gets tossed in loose comes back to you as a tuple. Whatever arrives with a name pinned to it comes back as a dict. You never have to know the count beforehand, which is the entire point.",
    when: "You are writing a function that should take a flexible number of inputs, or forwarding arguments straight through to another function.",
    code: 'def brew(*teas, **extras):\n    print(teas)      # tuple of positionals\n    print(extras)    # dict of keywords\nbrew("chai", "matcha", sugar=2, milk=True)',
    runes: ["def", "brew", "(", "*teas", "**extras", ")"],
    mission: "Write a function that takes *args and prints how many arguments it received.",
    gotcha: "Order is fixed: positional, then *args, then keyword, then **kwargs. And * unpacks a list into arguments while ** unpacks a dict. Easy to swap by accident." },
  { id: "lambdaux", name: "Lambda", school: "inscription", rank: 2, from: "functo",
    real: "lambda (anonymous functions)",
    desc: "A tiny one-line function with no name. Perfect when you need a quick throwaway rule to hand to another function.",
    say: "Scrawl the spell in the margin, use it once, let it fade.",
    image: "A script you scribble in the margin because naming it would take longer than using it. lambda x: x*2 is one expression, no name, handed straight to sort or map and forgotten by the next page.",
    when: "You need a short function for a single moment. Usually as a key= for sorting or an argument to map/filter.",
    code: 'double = lambda x: x * 2\nprint(double(5))            # 10\npairs.sort(key=lambda p: p[1])',
    runes: ["double", "=", "lambda x", ":", "x * 2"],
    mission: "Write a lambda that squares a number, assign it to a name, and call it.",
    gotcha: "A lambda holds one expression only. No statements, no multiple lines. If you are forcing it you wanted a real def; assigning a lambda to a name is usually that sign." },
  { id: "sortkey", name: "Sorting", school: "inscription", rank: 2, from: "listling",
    real: "Sorting with key=",
    desc: "Line items up by a rule you choose. key= names which property to sort on; reverse=True flips the direction; sorted() returns a new list, .sort() changes in place.",
    say: "Line the jars up by whatever measure I name.",
    image: "key= is a ruler you hold up to each jar in turn. Whatever the ruler reads is where the jar goes. The jars themselves never change and never learn the rule.",
    when: "You need a list in order by something other than its raw values. By a dict field, by length, by any computed measure.",
    code: 'orders = [{"drink": "chai", "sales": 30}]\norders.sort(key=lambda o: o["sales"], reverse=True)\ntop = sorted(names, key=len)',
    runes: ["orders.sort", "(", "key=lambda o", ")"],
    mission: "Sort a list of dictionaries by one of their values, highest first.",
    gotcha: ".sort() changes the list in place and returns None. So x = mylist.sort() gives you None. Use sorted() when you want a new list back." },
  { id: "enumzip", name: "enumerate and zip", school: "inscription", rank: 2, from: "condifork",
    real: "enumerate & zip",
    desc: "Two loop companions. enumerate hands you a running index with each item; zip walks two lists side by side, pairing them element for element.",
    say: "Walk the line counting. Or walk two lines in step.",
    image: "enumerate tags each item with its number as you pass; zip lays two lines beside each other and pairs them one-to-one, stopping when the shorter runs out.",
    when: "You need the position of each item while looping (enumerate), or you need to march through two lists together (zip).",
    code: 'for i, drink in enumerate(drinks):\n    print(i, drink)\nfor name, price in zip(names, prices):\n    print(name, price)',
    runes: ["for", "i, drink", "in", "enumerate(drinks)", ":"],
    mission: "Loop over a list with enumerate and print each item's index and value.",
    gotcha: "zip stops at the shortest input and silently drops the rest. Use itertools.zip_longest if you need every item. enumerate starts at 0 unless you pass a start." },
  { id: "strworks", name: "Strings", school: "inscription", rank: 1, from: "varibuddy",
    real: "String methods",
    desc: "The everyday tools for reshaping text: .strip() trims the edges, .split() breaks it into a list, .join() presses a list back together, .replace(), .upper(), .lower(), and more.",
    say: "Press the clay. Trim it, break it, rejoin it, however it must sit.",
    image: "Clay under your hands. .strip() trims the ragged edges, .split() breaks it into pieces, .join() presses the pieces back into one, .replace() swaps a part out.",
    when: "Almost any time you read text somebody else wrote. A CSV line, a filename, whatever a user typed into a box, and it needs cleaning before it's any use.",
    code: 'raw = "  Chai, Matcha, Cocoa  "\nitems = raw.strip().split(", ")\nline = " | ".join(items)\nprint(line.upper())',
    runes: ["raw", ".strip()", ".split(", ")", ".join()"],
    mission: "Take a messy string, strip it, split it into a list, then join it back with a different separator.",
    gotcha: "Strings are immutable: .strip(), .upper(), and .replace() return a new string, they do not change the original. s.strip() alone does nothing unless you write s = s.strip()." },
  { id: "modulox", name: "Imports", school: "transmutation", rank: 1,
    real: "Modules, imports & packages",
    desc: "Splits your craft across tomes and borrows from others. import, from…import, and the __main__ guard.",
    say: "From the pathlib tome, borrow Path.",
    image: "A library of tomes. 'import' pulls a whole book off the shelf; 'from … import' takes just the one page you need.",
    when: "You want a tool someone already built, or to split your code across files.",
    code: "from pathlib import Path\n\nif __name__ == '__main__':\n    main()",
    runes: ["from", "pathlib", "import", "Path"],
    mission: "Put a function in one file and import it into another.",
    gotcha: "from module import * dumps everything into your namespace and hides where names came from. Import explicitly. Naming a file after a library, like random.py, shadows the real one." },
  { id: "pathio", name: "Files", school: "transmutation", rank: 2,
    real: "File & path I/O (CSV / JSON)",
    desc: "A doorway to the outside world. Read and write. with open(), pathlib.Path, plus the csv and json tomes.",
    say: "Let 'text' be the contents read from laps.json.",
    image: "A doorway between your workshop and the world. Carry data in, set data out; 'with' closes the door behind you automatically.",
    when: "Your data lives in a file (csv/json/txt) and you need to read or save it.",
    code: "import json\ndata = json.loads(Path('scores.json').read_text())",
    runes: ["text", "=", 'Path("laps.json")', ".read_text()"],
    mission: "Read a small .json file and print one value from it.",
    gotcha: "Pass encoding='utf-8' when you open files, or you will hit odd errors on other machines. Relative paths depend on where you run the script, not where the file sits. Pathlib helps." },
  { id: "genryu", name: "Generators", school: "transmutation", rank: 2, from: "condifork",
    real: "Iterators & generators",
    desc: "A loop that yields one value at a time instead of conjuring the whole flood. Gentle on memory, endless in reach.",
    say: "Yield each line, stripped, one at a time.",
    image: "A quill that writes the next page only when you hold your hand out, and never writes ahead. The book can run forever because at any moment only one page of it exists.",
    when: "The data is huge or endless and you only need one piece at a time.",
    code: "def stream(path):\n    for line in open(path):\n        yield line.strip()",
    runes: ["yield", "line", ".strip()"],
    mission: "Write a generator that yields the squares of 1, 5 with yield.",
    gotcha: "A generator is single-use. Once you loop over it, it is empty. It also runs lazily, so an error inside may not surface until you actually iterate." },
  { id: "decorion", name: "Decorators", school: "transmutation", rank: 3, from: "functo",
    real: "Decorators",
    desc: "Wraps a function in something extra without opening it up. Caching, timing, a line in the log. @property and functools live here.",
    say: "Crown fib with the cache-charm, then define it.",
    image: "A cloak thrown over the knife. The blade underneath hasn't been touched and doesn't know. But now everything that passes through gets counted, or remembered, or written down on the way past.",
    when: "You want to add behavior (caching, timing, logging) to a function without editing its body.",
    code: "import functools\n\n@functools.lru_cache\ndef fib(n):\n    return n if n < 2 else fib(n-1) + fib(n-2)",
    runes: ["@functools.lru_cache", "def", "fib(n)", ":"],
    mission: "Add @functools.lru_cache to a slow recursive function and watch it fly.",
    gotcha: "A decorator replaces your function, so the wrapper can hide its name and docstring. Use functools.wraps to keep them. Stacked decorators apply bottom-up." },
  { id: "classalisk", name: "Classes", school: "transmutation", rank: 2,
    real: "Classes & OOP",
    desc: "A blueprint binding data and behavior together. __init__, methods, inheritance, and dunder rites like __repr__.",
    say: "Define the blueprint 'Driver'.",
    image: "A blueprint isn't a thing. It's the sheet the thing is drawn on. Stamp forty Drivers off one sheet and each carries its own name and number, but redraw a line on the sheet and you have redrawn all forty.",
    when: "You have many things of the same kind, each with its own data and actions.",
    code: "class Book:\n    def __init__(self, title, pages):\n        self.title, self.pages = title, pages\n    def __repr__(self):\n        return f'{self.title} ({self.pages}p)'",
    runes: ["class", "Driver", ":"],
    mission: "Make a Driver class with name + number and a readable __repr__.",
    gotcha: "Every method takes self as its first parameter, and attributes are self.x. Forgetting self is the number-one beginner error. A mutable class attribute is shared across all instances." },
  { id: "dataclassa", name: "Dataclasses", school: "transmutation", rank: 2, from: "classalisk",
    real: "Dataclasses & type hints",
    desc: "The blueprint with its boilerplate etched for you, plus type hints that document and guard against slips.",
    say: "Mark 'Driver' a dataclass: name is text, number is a whole number.",
    image: "The same sheet, but the tedious strokes are already inked. You write down what the fields are called and what kind of thing belongs in each, and everything repetitive underneath gets drawn for you.",
    when: "You want a simple class that mostly just holds labeled data.",
    code: "from dataclasses import dataclass\n\n@dataclass\nclass Student:\n    name: str\n    grade: int",
    runes: ["@dataclass", "class", "Driver", ":"],
    mission: "Rewrite your Driver class as a @dataclass.",
    gotcha: "Type hints are not enforced at runtime. X: int = 'hi' runs fine; they are for readers and tools. A dataclass with a mutable default needs field(default_factory=list), not = []." },
  { id: "venvurtle", name: "venv and pip", school: "transmutation", rank: 1,
    real: "Virtual environments & pip",
    desc: "A sealed circle so each script keeps its own reagents. venv, pip, requirements.txt. No cross-contamination.",
    say: "Make a sealed circle '.venv', step in, install pandas.",
    image: "Every project gets its own locked cupboard of reagents. Two projects wanting different versions of the same jar is the commonest quiet disaster in the craft, and a cupboard each is the whole of the cure.",
    when: "You start any new project (do this first, every single time).",
    code: "python -m venv .venv\nsource .venv/bin/activate\npip install pandas",
    runes: ["python", "-m", "venv", ".venv"],
    mission: "Create a venv, activate it, and pip install one package inside it.",
    gotcha: "pip install with no active venv installs system-wide and clashes between projects. Activate the venv first. requirements.txt pins versions so others match." },

  { id: "swornpact", name: "Protocols", school: "transmutation", rank: 3, from: "classalisk",
    real: "Interfaces (Protocols & ABCs)",
    desc: "A contract a class promises to keep. A Protocol says 'anything with these methods counts'; an abstract base class (ABC) forces subclasses to implement them. It's how you write code that works with many shapes at once.",
    say: "Swear the pact. Any familiar that can 'speak' may answer the call.",
    image: "A sworn oath. A Protocol doesn't care WHAT you are, only that you can do the named things, 'if it can .area(), it counts as a Shape.' An ABC is a stricter oath: forget a required method and the class won't even form.",
    when: "You want a function that accepts any object behaving a certain way, without caring about its exact class. Duck typing, made explicit and safe.",
    code: 'from typing import Protocol\n\nclass Drawable(Protocol):\n    def area(self) -> float: ...\n\ndef describe(shape: Drawable):\n    print(shape.area())   # any class with area() fits',
    runes: ["class", "Drawable", "(", "Protocol", ")"],
    mission: "Write a Protocol with one method, a class that has that method, and a function that accepts the Protocol.",
    gotcha: "A Protocol is checked by shape (does it have the methods?), an ABC by inheritance (did you subclass it?). Keep the two models separate. Every abstract method must be implemented or the class cannot be created." },
  { id: "withward", name: "Context Managers", school: "transmutation", rank: 2, from: "pathio",
    real: "Context managers (with)",
    desc: "A block that guarantees its own cleanup. with open(...) as f: opens the file and closes it the instant the block ends. Even if the code inside raises an error.",
    say: "Step through the door. It will shut behind you no matter what.",
    image: "A door with a weight on the hinge. It shuts whether you walked out or fell out. The with-block hands you the file on the way in and closes it behind you either way.",
    when: "Any time you open a file, database, or lock: the with-block frees you from ever forgetting to close or release it.",
    code: 'with open("orders.csv") as f:\n    data = f.read()\n# file is closed automatically here',
    runes: ["with", "open(...)", "as", "f", ":"],
    mission: "Open a file with a with-block and read it. With no manual close call.",
    gotcha: "The resource is only open inside the block. Using the file after the with ends raises an error, because it is already closed." },
  { id: "regexr", name: "Regex", school: "transmutation", rank: 3, from: "strworks",
    real: "Regular expressions (re)",
    desc: "Finds every scrap of text matching a shape you describe, without knowing in advance what any of it says. Every number in a file, or every address, or every date written four different ways. findall, search and sub do nearly all of it.",
    say: "Raise the lens. Reveal every shape that fits the pattern.",
    image: "Verrell ground a lens that finds shapes rather than words. Hold it over a wall of text, tell it \"three digits\" or \"anything with an @ in it\", and every place matching that shape lights up. It has no idea what any of it says.",
    when: "You need to extract or replace text by its shape rather than its exact value: pulling numbers out, validating a format, cleaning whitespace.",
    code: 'import re\ntext = "chai:30, matcha:25"\npairs = re.findall(r"(\\w+):(\\d+)", text)\n# [("chai", "30"), ("matcha", "25")]',
    runes: ["import re", "re.findall", "(", "pattern", ")"],
    mission: "Use re.findall with a simple pattern to pull all the numbers out of a string.",
    gotcha: "Always use a raw string for the pattern (the r prefix) so backslashes survive. Greedy .* grabs as much as it can. Use .*? when you want the shortest match." },
  { id: "toolkit", name: "collections and itertools", school: "transmutation", rank: 2, from: "listling",
    real: "collections & itertools",
    desc: "Two standard-library drawers most people never open. Counter tallies items, defaultdict fills missing keys, deque is a fast two-ended queue; itertools has chain, groupby, and more. Each turning ten lines into one.",
    say: "Open the drawer no one opens. The tool is already inside.",
    image: "The drawer nobody in the workshop opens because they assume it's full of junk. Counter has been tallying things since before you were born. defaultdict has never once complained about a missing key.",
    when: "Whenever you catch yourself hand-writing a tally, a grouping, or a missing-key guard. There is almost certainly a one-line tool for it here.",
    code: 'from collections import Counter\ntally = Counter(["chai", "chai", "matcha"])\nprint(tally.most_common(1))\n# most frequent item',
    runes: ["from collections import Counter", "Counter(...)", ".most_common(1)"],
    mission: "Use Counter to count how many times each item appears in a list.",
    gotcha: "Most itertools functions return a one-use iterator, not a list. Wrap it in list() to see or reuse it. And groupby only groups consecutive items, so sort first." },
  { id: "numpyra", name: "NumPy", school: "divination", rank: 3,
    real: "NumPy",
    desc: "Fast numeric arrays with the maths done on the whole cloth at once. Vectorized and broadcast, with no loop anywhere in sight.",
    say: "Let 'arr' be a numeric array of the speeds.",
    image: "A loom that does math on the whole cloth at once. No threading one number at a time; the entire array shifts together.",
    when: "You're doing math over many numbers and want it fast.",
    code: "import numpy as np\nspeeds = np.array(speed_list)\nspeeds.mean(), speeds.max()",
    runes: ["arr", "=", "np.array", "(speeds)"],
    mission: "Make a NumPy array of five speeds and print its mean and max.",
    gotcha: "A NumPy array has one fixed dtype, so mixing types silently upcasts (ints become floats). And a slice is a view, not a copy. Editing it edits the original array." },
  { id: "pandasaur", name: "pandas", school: "divination", rank: 3, from: "numpyra",
    real: "pandas",
    desc: "The great ledger of data-craft: DataFrames, groupby, merge, filter, apply. A spreadsheet bent to your will.",
    say: "Take df, group by drink, read price, average it.",
    image: "A great ledger of rows and columns you can sort, group, and question. A spreadsheet that obeys spoken commands.",
    when: "Your data is table-shaped and you need to filter, group, or summarize it.",
    code: "import pandas as pd\ndf.groupby('drink')['price'].mean()",
    runes: ["df", ".groupby", '("drink")', '["price"]', ".mean()"],
    mission: "Load any CSV into a DataFrame and groupby one column.",
    gotcha: "Chained indexing like df[df.x > 0]['y'] = 1 may not stick and warns you. Use .loc[rows, col] instead. Most operations return a new frame unless you assign it back." },
  { id: "plotwing", name: "Matplotlib", school: "divination", rank: 2,
    real: "Matplotlib",
    desc: "Turns numbers into a picture you can read at a glance. line / scatter / bar, subplots, labels, styling.",
    say: "Plot speed against distance; label the x-axis.",
    image: "Charcoal on the back of an envelope. Rough, fast, ugly, and it tells you in one second what a column of four hundred numbers would take you an hour to feel.",
    when: "You want a quick static chart to see what the numbers are doing.",
    code: "import matplotlib.pyplot as plt\nplt.plot(days, sales)\nplt.xlabel('Day')",
    runes: ["plt", ".plot", "(", "dist, speed", ")"],
    mission: "Plot a line chart of ten points and label both axes.",
    gotcha: "Figures pile up in memory if you never close them. Call plt.close() inside loops. And plt.show() blocks until you close the window, and does nothing in a script that exits right away." },
  { id: "plotlynx", name: "Plotly", school: "divination", rank: 3, from: "plotwing",
    real: "Plotly (interactive charts)",
    desc: "Scrying-glass you can zoom and hover. Perfect for laying two drinks' sales side by side.",
    say: "Make a line of sales over days, one color per drink.",
    image: "The same drawing, except it talks back. Put a finger on any point and it tells you its exact value, which means you stop squinting at axes and start asking it questions.",
    when: "You want an interactive chart, especially inside a dashboard.",
    code: "import plotly.express as px\nfig = px.line(df, x='day', y='sales', color='drink')",
    runes: ["px", ".line", "(", 'df, x="day", y="sales"', ")"],
    mission: "Make a px.line chart colored by a category column.",
    gotcha: "Plotly draws with HTML and JavaScript, so it needs a browser or notebook to appear. It will not pop up in a bare terminal. Very large datasets make the figure sluggish." },
  { id: "fastfoxx", name: "Reading CSVs", school: "divination", rank: 3,
    real: "Reading a data file (read_csv)",
    desc: "Summons a whole dataset from a file. Every row and column, ready to explore. The first step of any data project.",
    say: "Read orders.csv into a table; peek at the first rows.",
    image: "You name the file and the entire thing comes up at once. Every row, every column, nothing left in the water. Which is exactly why the next thing you do is look hard at what you caught.",
    when: "You need to load real data from a file to begin.",
    code: "import pandas as pd\ndf = pd.read_csv('orders.csv')\ndf.head()",
    runes: ["df", "=", "pd.read_csv", '("orders.csv")'],
    mission: "Read a CSV and print how many rows it has.",
    gotcha: "read_csv guesses types, so it may read IDs as numbers (dropping leading zeros) or dates as plain text. Check df.dtypes and pass dtype= or parse_dates= when needed." },
  { id: "streamlet", name: "Streamlit", school: "divination", rank: 3,
    real: "Streamlit",
    desc: "Weaves a plain script into a shareable window with a few lines. Widgets, layout, caching, deploy.",
    say: "Make a drink picker; draw the chart in the window.",
    image: "Your script has been sitting on a bench in the back room. This puts it behind glass on the street with a handle on it, so a stranger can walk up and turn the handle without ever learning what a script is.",
    when: "You want to turn a script into a shareable app with buttons and charts.",
    code: "import streamlit as st\nd = st.selectbox('Drink', drinks)\nst.plotly_chart(fig)",
    runes: ["st", ".plotly_chart", "(", "fig", ")"],
    mission: "Make a five-line Streamlit app with a selectbox and st.write.",
    gotcha: "Streamlit re-runs the whole script top to bottom on every click. Cache expensive work with st.cache_data. State resets each run unless you use st.session_state." },
  { id: "jupytl", name: "Jupyter", school: "divination", rank: 1,
    real: "Jupyter notebooks",
    desc: "Explore data cell by cell, see charts inline, then etch the good strokes into real .py tomes.",
    say: "Show the first rows; draw charts inline.",
    image: "A scrying table where you test one spell per cell and watch it work before committing it to a real page.",
    when: "You're exploring data and want to try things step by step.",
    code: "# in a notebook cell\ndf.head()\n%matplotlib inline",
    runes: ["df", ".head", "(", ")"],
    mission: "Open a notebook, load a DataFrame, and call .head() in a cell.",
    gotcha: "Cells share state and can run out of order, so a notebook that looks fine may break top to bottom. Restart and run-all to be sure. Leftover state from deleted cells is a classic trap." },

  { id: "gitalon", name: "Git", school: "artifice", rank: 2,
    real: "Git & GitHub",
    desc: "Keeps every version of your work and shows it to the world. commit, branch, push, PRs, .gitignore.",
    say: "Stage everything; seal it with the note “add sales chart”; send it out.",
    image: "A ledger that keeps every version of the page rather than the latest one. Any afternoon you ruined, you can walk back to the morning before it. GitHub is where you hang the ledger somewhere people can read it.",
    when: "You want to save versions of your work and never lose it (and show it off).",
    code: "git add .\ngit commit -m 'feat: add sales chart'\ngit push",
    runes: ["git", "commit", "-m", '"add sales chart"'],
    mission: "Init a repo, make one commit, and push it to GitHub.",
    gotcha: "Commit only snapshots what you staged with git add. Files you forgot to add are left out. And never commit secrets or huge data; a .gitignore should catch them first." },
  { id: "pytestrix", name: "pytest", school: "artifice", rank: 2,
    real: "Testing with pytest",
    desc: "A rite that recasts your spells after every change and tells you the moment one stops behaving. Assertions, fixtures, and a coverage number you're allowed to argue with.",
    say: "Define a test for delta; assert it returns “0.2”.",
    image: "Someone in the next room recasting everything you wrote last month, over and over, and knocking on the wall the second one of them fails.",
    when: "You want to be sure a change didn't quietly break something.",
    code: "def test_delta():\n    assert delta(9.4, 9.2) == '0.2'",
    runes: ["def", "test_delta", "()", ":"],
    mission: "Write one test asserting your delta() returns the right string.",
    gotcha: "pytest only collects functions and files named test_ something, and silently skips the rest. A test that asserts nothing always passes. Make each assert actually check something." },
  { id: "requestro", name: "APIs", school: "artifice", rank: 2,
    real: "APIs & requests",
    desc: "Calls across the web and hears back. HTTP requests, JSON replies, headers, params. Speak to any service.",
    say: "Call the url with a page number; read the reply as data.",
    image: "You send a note to a house you've never visited and a stranger there sends back a scroll. You don't know who wrote it, how long they took, or whether they'll answer at all next time.",
    when: "You need data from a website or online service.",
    code: "import requests\nr = requests.get(url, params={'page': 2})\ndata = r.json()",
    runes: ["r", "=", "requests.get", "(url)"],
    mission: "GET a public JSON API and print one field from the reply.",
    gotcha: "A response can come back with an error status like 404 or 500 and still count as a successful request. Call resp.raise_for_status(). And always set a timeout, or a stalled server hangs your program." },
  { id: "autogolem", name: "Automation", school: "artifice", rank: 2,
    real: "Automation & scripting",
    desc: "A familiar that does the dull work: batch-renames, scrapes, glues tools, runs on a schedule while you rest.",
    say: "For each csv the glob finds in this folder, process it.",
    image: "Something small and dull-witted that does the boring half of your job at three in the morning and never once asks why.",
    when: "You're doing the same boring task by hand more than twice.",
    code: "from pathlib import Path\nfor f in Path('.').glob('*.csv'):\n    process(f)",
    runes: ["for", "f", "in", 'Path(".").glob("*.csv")', ":"],
    mission: "Write a loop that processes every .csv in a folder.",
    gotcha: "A script that half-runs then crashes can leave things in a broken state. Make each step safe to re-run. Test on a copy before automating anything destructive." },
  { id: "debuggite", name: "Debugging", school: "artifice", rank: 1,
    real: "Debugging & profiling",
    desc: "Finds where a spell breaks or drags. breakpoint(), logging, reading tracebacks, timeit to measure speed.",
    say: "Log, at info level, that the data loaded.",
    image: "Stopping the script mid-cast and going along the line asking every value what it actually is, rather than what you assumed it was. It's slow. It's always faster than guessing.",
    when: "Something's broken or slow and you need to see what's really happening.",
    code: "import logging\nlogging.basicConfig(level=logging.INFO)\nlogging.info('data loaded ok')",
    runes: ["logging", ".info", "(", "msg", ")"],
    mission: "Add a breakpoint() to a function and inspect a variable when it pauses.",
    gotcha: "print debugging works but scatters noise everywhere. Breakpoint() lets you inspect live instead. And optimize only what you have measured; the slow part is rarely where you guess." },
  { id: "asyncat", name: "Async", school: "artifice", rank: 3,
    real: "Async & concurrency",
    desc: "Sends a hundred slow spells out without waiting on each one to come back. async and await, and the difference between threads and processes.",
    say: "Define an async fetch that awaits the reply.",
    image: "Sending out forty notes on the same morning instead of waiting by the door for each reply before writing the next. The writing was never the slow part. The waiting was.",
    when: "You have many slow waits (network calls) and want them to overlap.",
    code: "import asyncio\nasync def fetch(u):\n    return await client.get(u)",
    runes: ["async", "def", "fetch(u)", ":"],
    mission: "Write an async function and run it with asyncio.run().",
    gotcha: "await only works inside an async def, and calling an async function without awaiting just makes a coroutine that never runs. Async helps with waiting (I/O), not heavy computation. That needs multiprocessing." },
];

export const LABYRINTH = [
  { id: "threshold", name: "First Crossing", tier: "First Labyrinth",
    desc: "Clear it and you can read and write basic spells unaided.",
    team: ["varibuddy", "condifork", "listling", "functo"],
    code: `# First Crossing. What your kit adds up to
scores = {"Ada": 88, "Bo": 92, "Cy": 79}   # Lists and Dicts

def top(entries):                         # Functions
    best_name, best_score = None, 0
    for name, s in entries.items():       # Control Flow
        if s > best_score:                # Control Flow
            best_name, best_score = name, s
    return best_name, best_score

name, s = top(scores)
print(f"Top: {name} with {s}")            # Variables` },
  { id: "automaton", name: "The Automaton's Vault", tier: "Field Labyrinth",
    desc: "A script that chews through files with nobody watching.",
    team: ["pathio", "modulox", "trycatchu", "autogolem"],
    code: `# The Automaton's Vault. Runs with nobody watching
import json                               # Imports
from pathlib import Path                   # Imports + Files

def process(path):
    return json.loads(path.read_text())   # Files

for f in Path("data").glob("*.json"):     # Automation
    try:                                  # Exceptions
        record = process(f)
        print(f"{f.name}: {record['title']}")
    except (ValueError, KeyError):        # one bad file should not stop the other four hundred
        print(f"{f.name}: skipped (bad file)")` },
  { id: "codex", name: "The Open Codex", tier: "Endgame Labyrinth",
    desc: "Work clean enough that a stranger could pick it up and add to it.",
    team: ["gitalon", "pytestrix", "dataclassa", "debuggite", "pandasaur"],
    code: `# The Open Codex. Tested, typed, kept
import logging                            # Debugging
from dataclasses import dataclass         # Dataclasses

logging.basicConfig(level=logging.INFO)

@dataclass                                # Dataclasses
class Score:
    player: str
    points: float

def delta(a: Score, b: Score) -> float:    # Functions
    logging.info(f"comparing {a.player} vs {b.player}")   # Debugging
    return round(a.points - b.points, 3)

def test_delta():                         # pytest
    assert delta(Score("Ada", 9.4), Score("Bo", 9.2)) == 0.2

# git commit -m "add delta"   <- Git` },
];

export const RANKS = [
  [37, "Fluent"], [30, "Confident"], [22, "Working knowledge"],
  [14, "Getting somewhere"], [6, "Started"], [0, "Day one"],
];

export const ACTS = [
  { school: "inscription", numeral: "I", title: "The First Inks" },
  { school: "transmutation", numeral: "II", title: "What Bends" },
  { school: "divination", numeral: "III", title: "Reading the Signs" },
  { school: "artifice", numeral: "IV", title: "What Lasts" },
];

export const LAB_NOTE = {
  threshold: "You can read and write basic Python without looking anything up.",
  automaton: "That script runs at three in the morning with nobody watching it.",
  codex: "A stranger could open this and trust it without knowing the first thing about you.",
};

export const SPELL_HANDS = {
  argstar: [
    { ctx: "Many inputs", code: 'def total(*nums):\n    return sum(nums)\ntotal(1, 2, 3, 4)', note: "*nums gathers every positional argument into a tuple you can loop or sum.", runes: ["def", "total", "(", "*nums", ")"] },
    { ctx: "Named extras", code: 'def brew(tea, **opts):\n    print(tea, opts)\nbrew("chai", sugar=2)', note: "**opts collects any keyword arguments into a dictionary.", runes: ["def", "brew", "(", "tea", "**opts", ")"] },
    { ctx: "Passing along", code: 'def wrap(*a, **k):\n    log(*a, **k)', note: "Star-unpacking forwards all arguments to another function unchanged.", runes: ["log", "(", "*a", "**k", ")"] },
  ],
  lambdaux: [
    { ctx: "Tiny function", code: 'square = lambda x: x * x\nsquare(6)   # 36', note: "A lambda is a one-line function with no name unless you assign it.", runes: ["lambda", "x", ":", "x * x"] },
    { ctx: "As a key", code: 'words.sort(key=lambda w: len(w))', note: "Lambdas shine as throwaway key= functions for sorting.", runes: ["key=", "lambda w", ":", "len(w)"] },
    { ctx: "With map", code: 'doubled = list(map(lambda n: n * 2, nums))', note: "Pass a lambda straight into map or filter without defining it first.", runes: ["map", "(", "lambda n: n * 2", ")"] },
  ],
  sortkey: [
    { ctx: "By value", code: 'orders.sort(key=lambda o: o["sales"])', note: "key= tells sort which field to order by. Here, sales.", runes: ["sort", "(", "key=lambda o", ")"] },
    { ctx: "Reverse", code: 'top = sorted(nums, reverse=True)', note: "reverse=True flips to highest-first; sorted() returns a fresh list.", runes: ["sorted", "(", "nums", "reverse=True", ")"] },
    { ctx: "By length", code: 'names.sort(key=len)', note: "Any function works as a key. Len sorts by string length.", runes: ["names.sort", "(", "key=len", ")"] },
  ],
  enumzip: [
    { ctx: "Index + item", code: 'for i, x in enumerate(items):\n    print(i, x)', note: "enumerate gives a counter alongside each item. No manual i += 1.", runes: ["enumerate", "(", "items", ")"] },
    { ctx: "Start at 1", code: 'for n, x in enumerate(items, 1):\n    print(n, x)', note: "The second argument sets where the count begins.", runes: ["enumerate", "(", "items", ",", "1", ")"] },
    { ctx: "Two lists", code: 'for name, price in zip(names, prices):\n    print(name, price)', note: "zip pairs two lists element-by-element and stops at the shorter one.", runes: ["zip", "(", "names", ",", "prices", ")"] },
  ],
  strworks: [
    { ctx: "Clean up", code: '"  hi  ".strip()   # "hi"', note: ".strip() removes whitespace, or given characters, from both ends.", runes: ['"  hi  "', ".strip()"] },
    { ctx: "Split", code: '"a,b,c".split(",")   # ["a", "b", "c"]', note: ".split() breaks a string into a list on a separator.", runes: ['"a,b,c"', ".split", '(",")'] },
    { ctx: "Join", code: '", ".join(["a", "b"])   # "a, b"', note: ".join() glues a list of strings back into one.", runes: ['", "', ".join", "(", "items", ")"] },
  ],
  withward: [
    { ctx: "Read a file", code: 'with open("data.txt") as f:\n    text = f.read()', note: "The file closes automatically when the block ends, even on error.", runes: ["with", "open(...)", "as", "f", ":"] },
    { ctx: "Write", code: 'with open("out.txt", "w") as f:\n    f.write("hi")', note: "Same guarantee for writing. No forgotten close call.", runes: ["with", "open(..., mode)", "as", "f"] },
    { ctx: "Why it matters", code: '# without with, a crash mid-read\n# can leave the file open', note: "Context managers make cleanup automatic and crash-safe.", runes: ["with", "...", "as", "f", ":"] },
  ],
  regexr: [
    { ctx: "Find all", code: 'import re\nre.findall(r"\\d+", "a1b22")   # ["1", "22"]', note: "The pattern matches runs of digits; findall returns every match.", runes: ["re.findall", "(", "pattern", ")"] },
    { ctx: "Search", code: 'm = re.search(r"(\\w+)@", email)', note: "search finds the first match; the parentheses capture a group.", runes: ["re.search", "(", "pattern", ")"] },
    { ctx: "Replace", code: 're.sub(r"\\s+", " ", messy)', note: "sub swaps every match. Here, collapsing runs of whitespace.", runes: ["re.sub", "(", "pattern", ")"] },
  ],
  toolkit: [
    { ctx: "Count", code: 'from collections import Counter\nCounter("aabbbc")', note: "Counter tallies how many of each item in a single line.", runes: ["Counter", "(", "items", ")"] },
    { ctx: "Default", code: 'from collections import defaultdict\nd = defaultdict(list)\nd["x"].append(1)', note: "defaultdict fills missing keys automatically. No KeyError.", runes: ["defaultdict", "(", "list", ")"] },
    { ctx: "Chain", code: 'from itertools import chain\nlist(chain([1, 2], [3, 4]))', note: "itertools.chain flattens several iterables into one stream.", runes: ["chain", "(", "a", ",", "b", ")"] },
  ],
  mirrorrite: [
    { ctx: "Flat list", code: 'a = [1, 2, 3]\nb = a[:]        # or a.copy()\nb.append(4)     # a is untouched', note: "For a flat list, slicing [:] or .copy() gives a safe, independent copy.", runes: ["b", "=", "a[:]"] },
    { ctx: "Nested", code: 'import copy\nb = copy.deepcopy(a)', note: "deepcopy clones every level, so nested lists/dicts become fully independent.", runes: ["copy", ".deepcopy", "(", "a", ")"] },
    { ctx: "Gotcha", code: 'b = a.copy()\nb[0].append(9)   # a[0] changed too!', note: "A shallow copy shares the inner objects. The classic surprise bug.", runes: ["b", "=", "a.copy()"] },
  ],
  nestedra: [
    { ctx: "Records", code: 'rows = [{"drink": "chai", "sales": 30}]\nprint(rows[0]["sales"])   # 30', note: "Index the list first ([0]), then key into that dict.", runes: ["rows", "[0]", '["sales"]'] },
    { ctx: "Grouping", code: 'by_type = {"hot": ["chai", "cocoa"]}\nby_type["hot"].append("tea")', note: "A dict whose values are lists. Key in, then use list methods on the result.", runes: ["by_type", '["hot"]', ".append"] },
    { ctx: "Looping", code: 'for r in rows:\n    print(r["drink"], r["sales"])', note: "Loop the outer list; each r is a dict you can key into.", runes: ["for", "r", "in", "rows", ":"] },
  ],
  swornpact: [
    { ctx: "Protocol", code: 'from typing import Protocol\n\nclass Named(Protocol):\n    name: str', note: "A Protocol just lists what something must have. No inheritance needed.", runes: ["class", "Named", "(", "Protocol", ")"] },
    { ctx: "ABC", code: 'from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self): ...', note: "An ABC forces every subclass to implement area(), or it cannot be created.", runes: ["@abstractmethod", "def", "area"] },
    { ctx: "Duck typing", code: 'def total_area(shapes):\n    return sum(s.area() for s in shapes)', note: "This works on ANY object with .area(). The interface in action.", runes: ["sum", "(", "s.area()", ")"] },
  ],
  varibuddy: [
    { ctx: "Games", code: 'hero = "Luna"\nprint(f"{hero} wins the match")', note: "Store a value under a name, then drop it into a sentence with an f-string.", runes: ["hero", "=", '"Luna"'] },
    { ctx: "Everyday", code: 'age = 19\nprint(f"You are {age} today")', note: "Numbers work the same way; {age} pours the value into the text.", runes: ["print", "(", 'f"You are {age} today"', ")"] },
    { ctx: "Money", code: 'price = 4.5\nprint(f"Costs ${price:.2f}")', note: "The :.2f formats a number to two decimals. Perfect for prices.", runes: ["price", "=", "4.5"] },
  ],
  condifork: [
    { ctx: "Kitchen", code: 'for step in range(1, 6):\n    print(f"Step {step}")', note: "A for-loop runs its body once per item. Here, steps 1 to 5.", runes: ["for", "step", "in", "range(1, 6)", ":"] },
    { ctx: "Weather", code: 'if temp > 30:\n    print("Hot")\nelse:\n    print("Mild")', note: "if/else takes exactly one branch depending on the condition.", runes: ["if", "temp", ">", "30", ":"] },
    { ctx: "Games", code: 'score = 0\nwhile score < 100:\n    score += 10', note: "A while-loop repeats until its condition turns false.", runes: ["while", "score", "<", "100", ":"] },
  ],
  listling: [
    { ctx: "Music", code: 'playlist = ["Intro", "Verse", "Chorus"]\nprint(playlist[0])', note: "A list holds items in order; index [0] is the first.", runes: ["playlist", "=", '["Intro", "Verse", "Chorus"]'] },
    { ctx: "Contacts", code: 'ages = {"Ana": 20, "Sam": 22}\nprint(ages["Ana"])', note: "A dict looks things up by a key instead of a position.", runes: ["ages", "[", '"Ana"', "]"] },
    { ctx: "School", code: 'subjects = {"math", "art", "math"}\nprint(len(subjects))', note: "A set silently drops duplicates. This prints 2, not 3.", runes: ["subjects", "=", "{", '"math", "art"', "}"] },
  ],
  comprehendra: [
    { ctx: "Shop", code: 'cheap = [p for p in prices if p < 5]', note: "Build a new list in one line, keeping only prices under 5.", runes: ["[", "p", "for", "p", "in", "prices", "]"] },
    { ctx: "Text", code: 'caps = [w.upper() for w in words]', note: "Transform every item at once. Here, uppercasing each word.", runes: ["[", "w.upper()", "for", "w", "in", "words", "]"] },
    { ctx: "School", code: 'lengths = {len(w) for w in words}', note: "Swap brackets for braces and you get a set of unique results.", runes: ["{", "len(w)", "for", "w", "in", "words", "}"] },
  ],
  functo: [
    { ctx: "Everyday", code: 'def delta(a, b):\n    return round(a - b, 3)', note: "Bind reusable logic to a name; call delta(...) anytime after.", runes: ["def", "delta", "(a, b)", ":"] },
    { ctx: "Greeting", code: 'def greet(name):\n    return f"Hi, {name}"', note: "Inputs go in the parentheses; the result comes back with return.", runes: ["def", "greet", "(name)", ":"] },
    { ctx: "Money", code: 'def total(price, qty=1):\n    return price * qty', note: "A default (qty=1) lets you skip that argument when you want.", runes: ["def", "total", "(price, qty=1)", ":"] },
  ],
  trycatchu: [
    { ctx: "Input", code: 'try:\n    n = int(text)\nexcept ValueError:\n    n = 0', note: "If int() chokes on bad text, the net catches it and sets 0.", runes: ["try", ":", "except", "ValueError", ":"] },
    { ctx: "Files", code: 'try:\n    data = open("laps.csv")\nexcept FileNotFoundError:\n    data = None', note: "You can catch a specific failure. Here, a missing file.", runes: ["except", "FileNotFoundError", ":"] },
    { ctx: "Math", code: 'try:\n    r = a / b\nexcept ZeroDivisionError:\n    r = 0', note: "Dividing by zero would crash; the ward hands back 0 instead.", runes: ["try", ":", "r", "=", "a / b"] },
  ],
  modulox: [
    { ctx: "Paths", code: 'from pathlib import Path\nprint(Path.cwd())', note: "Borrow one tool (Path) from a library and use it right away.", runes: ["from", "pathlib", "import", "Path"] },
    { ctx: "Math", code: 'import math\nprint(math.sqrt(16))', note: "import pulls in a whole module; reach into it with a dot.", runes: ["import", "math"] },
    { ctx: "Random", code: 'from random import choice\npick = choice(names)', note: "from...import grabs just the name you need, nothing else.", runes: ["from", "random", "import", "choice"] },
  ],
  pathio: [
    { ctx: "Read", code: 'text = Path("laps.txt").read_text()', note: "Open a file and pull all its text in a single stroke.", runes: ["text", "=", 'Path("laps.txt")', ".read_text()"] },
    { ctx: "Write", code: 'Path("out.txt").write_text("done")', note: "The same doorway works outward. Writing text to a file.", runes: ['Path("out.txt")', ".write_text", "(", '"done"', ")"] },
    { ctx: "JSON", code: 'import json\ndata = json.loads(raw)', note: "json.loads turns a JSON string into a Python dict or list.", runes: ["data", "=", "json.loads", "(", "raw", ")"] },
  ],
  genryu: [
    { ctx: "Files", code: 'def lines(path):\n    for line in open(path):\n        yield line.strip()', note: "yield hands back one line at a time instead of the whole file.", runes: ["yield", "line", ".strip()"] },
    { ctx: "Numbers", code: 'def squares(n):\n    for i in range(n):\n        yield i * i', note: "Each request produces the next square, lazily, on demand.", runes: ["yield", "i", "*", "i"] },
    { ctx: "Inline", code: 'gen = (x * 2 for x in nums)', note: "Round brackets make a generator expression. A lazy comprehension.", runes: ["(", "x * 2", "for", "x", "in", "nums", ")"] },
  ],
  decorion: [
    { ctx: "Cache", code: '@functools.lru_cache\ndef fib(n):\n    return fib(n-1) + fib(n-2)', note: "The @charm wraps fib so it remembers results and runs fast.", runes: ["@functools.lru_cache", "def", "fib(n)", ":"] },
    { ctx: "Property", code: 'class Car:\n    @property\n    def kmh(self):\n        return self.mph * 1.6', note: "@property lets a method be read like a plain attribute.", runes: ["@property", "def", "kmh(self)", ":"] },
    { ctx: "Timing", code: '@timer\ndef load():\n    ...', note: "Any decorator above a def adds behavior without touching its body.", runes: ["@timer", "def", "load()", ":"] },
  ],
  classalisk: [
    { ctx: "Games", code: 'class Hero:\n    def __init__(self, name):\n        self.name = name', note: "A class is a blueprint; __init__ sets up each new object.", runes: ["class", "Hero", ":"] },
    { ctx: "Method", code: 'class Dog:\n    def bark(self):\n        return "Woof"', note: "Functions inside a class are methods, called on the object.", runes: ["def", "bark(self)", ":"] },
    { ctx: "Display", code: 'class Lap:\n    def __repr__(self):\n        return f"Lap {self.n}"', note: "__repr__ decides how the object prints. Great for debugging.", runes: ["def", "__repr__(self)", ":"] },
  ],
  dataclassa: [
    { ctx: "School", code: '@dataclass\nclass Student:\n    name: str\n    grade: int', note: "One decorator writes all the boilerplate; you just name fields.", runes: ["@dataclass", "class", "Student", ":"] },
    { ctx: "Point", code: '@dataclass\nclass Point:\n    x: float\n    y: float', note: "Type hints (float) document each field and catch slips early.", runes: ["x", ":", "float"] },
    { ctx: "Default", code: '@dataclass\nclass Team:\n    name: str\n    wins: int = 0', note: "Give a field a default (= 0) just like a function argument.", runes: ["wins", ":", "int", "=", "0"] },
  ],
  venvurtle: [
    { ctx: "Create", code: 'python -m venv .venv', note: "Make a sealed environment folder named .venv for this project.", runes: ["python", "-m", "venv", ".venv"] },
    { ctx: "Activate", code: 'source .venv/bin/activate', note: "Step inside it so installs stay local to this project.", runes: ["source", ".venv/bin/activate"] },
    { ctx: "Install", code: 'pip install pandas', note: "With the circle active, pip adds packages only in here.", runes: ["pip", "install", "pandas"] },
  ],
  numpyra: [
    { ctx: "Numbers", code: 'import numpy as np\nprices = np.array(vals)', note: "Wrap a list in np.array to open fast, whole-array math.", runes: ["prices", "=", "np.array", "(vals)"] },
    { ctx: "Stats", code: 'print(speeds.mean(), speeds.max())', note: "One call runs across every number at once. No loop needed.", runes: ["speeds", ".mean", "(", ")"] },
    { ctx: "Vector", code: 'kmh = speeds * 3.6', note: "Multiplying scales the entire array in a single stroke.", runes: ["kmh", "=", "speeds", "*", "3.6"] },
  ],
  pandasaur: [
    { ctx: "Group", code: 'df.groupby("drink")["sales"].sum()', note: "Group rows by drink, then total each group's sales.", runes: ["df", ".groupby", '("drink")', '["sales"]', ".sum()"] },
    { ctx: "Filter", code: 'big = df[df["sales"] > 20]', note: "Keep only the rows where the condition is true.", runes: ["df", "[", 'df["sales"] > 20', "]"] },
    { ctx: "Load", code: 'df = pd.read_csv("laps.csv")', note: "read_csv loads a whole spreadsheet into a DataFrame.", runes: ["df", "=", "pd.read_csv", "(", '"laps.csv"', ")"] },
  ],
  plotwing: [
    { ctx: "Line", code: 'plt.plot(dist, speed)\nplt.xlabel("Distance")', note: "Plot y against x, then label the axis so it reads clearly.", runes: ["plt", ".plot", "(", "dist, speed", ")"] },
    { ctx: "Scatter", code: 'plt.scatter(laps, times)', note: "Swap plot for scatter to see individual points, not a line.", runes: ["plt", ".scatter", "(", "laps, times", ")"] },
    { ctx: "Bar", code: 'plt.bar(names, wins)', note: "bar draws one bar per category. Good for counts.", runes: ["plt", ".bar", "(", "names, wins", ")"] },
  ],
  plotlynx: [
    { ctx: "Line", code: 'fig = px.line(df, x="day", y="sales", color="drink")', note: "One interactive line per drink, hoverable and zoomable.", runes: ["px", ".line", "(", 'df, x="day", y="sales"', ")"] },
    { ctx: "Scatter", code: 'fig = px.scatter(df, x="day", y="sales")', note: "px.scatter makes an interactive point cloud from a DataFrame.", runes: ["px", ".scatter", "(", 'df, x="day", y="sales"', ")"] },
    { ctx: "Show", code: 'fig.show()', note: "fig.show() opens the chart in your browser to explore.", runes: ["fig", ".show", "(", ")"] },
  ],
  fastfoxx: [
    { ctx: "Load", code: 'df = pd.read_csv("orders.csv")', note: "Read a whole CSV file into a table in one line.", runes: ["df", "=", "pd.read_csv", '("orders.csv")'] },
    { ctx: "Peek", code: 'df.head()', note: "Peek at the first few rows to see the shape.", runes: ["df", ".head", "(", ")"] },
    { ctx: "Size", code: 'rows, cols = df.shape', note: "How big is the data. Rows and columns?", runes: ["df", ".shape"] },
  ],
  streamlet: [
    { ctx: "Widget", code: 'd = st.selectbox("Drink", drinks)', note: "A selectbox gives your app a dropdown to pick from.", runes: ["st", ".selectbox", "(", '"Drink", drinks', ")"] },
    { ctx: "Chart", code: 'st.plotly_chart(fig)', note: "Drop a Plotly figure straight into the app window.", runes: ["st", ".plotly_chart", "(", "fig", ")"] },
    { ctx: "Text", code: 'st.title("Speed Trace")\nst.write(df)', note: "st.title and st.write put a heading and a table on screen.", runes: ["st", ".title", "(", '"Speed Trace"', ")"] },
  ],
  jupytl: [
    { ctx: "Peek", code: 'df.head()', note: "head() shows the first few rows so you can eyeball the data.", runes: ["df", ".head", "(", ")"] },
    { ctx: "Summary", code: 'df.describe()', note: "describe() summarizes each column's stats in one cell.", runes: ["df", ".describe", "(", ")"] },
    { ctx: "Inline", code: '%matplotlib inline', note: "This magic makes charts appear right below the cell.", runes: ["%matplotlib", "inline"] },
  ],
  gitalon: [
    { ctx: "Save", code: 'git add .\ngit commit -m "add sales chart"', note: "Stage your changes, then seal them with a message.", runes: ["git", "commit", "-m", '"add sales chart"'] },
    { ctx: "Share", code: 'git push', note: "push sends your commits up to GitHub for the world to see.", runes: ["git", "push"] },
    { ctx: "Branch", code: 'git checkout -b feature', note: "Make and switch to a new branch to work safely aside.", runes: ["git", "checkout", "-b", "feature"] },
  ],
  pytestrix: [
    { ctx: "Basic", code: 'def test_delta():\n    assert delta(3, 1) == 2', note: "A test function asserts what the answer should be.", runes: ["def", "test_delta", "()", ":"] },
    { ctx: "Equal", code: 'assert greet("Ana") == "Hi, Ana"', note: "assert passes silently if true, and shouts if false.", runes: ["assert", 'greet("Ana")', "==", '"Hi, Ana"'] },
    { ctx: "Run", code: 'pytest -q', note: "Run pytest in the terminal to check every test at once.", runes: ["pytest", "-q"] },
  ],
  requestro: [
    { ctx: "Get", code: 'r = requests.get(url)\ndata = r.json()', note: "Fetch a page, then read its JSON reply as Python data.", runes: ["r", "=", "requests.get", "(url)"] },
    { ctx: "Params", code: 'r = requests.get(url, params={"year": 2024})', note: "params adds a query string cleanly to the request.", runes: ["params", "=", '{"year": 2024}'] },
    { ctx: "Check", code: 'print(r.status_code)', note: "status_code tells you if the call worked (200) or not.", runes: ["print", "(", "r.status_code", ")"] },
  ],
  autogolem: [
    { ctx: "Files", code: 'for f in Path(".").glob("*.csv"):\n    process(f)', note: "Loop over every CSV in a folder and handle each one.", runes: ["for", "f", "in", 'Path(".").glob("*.csv")', ":"] },
    { ctx: "Rename", code: 'for f in folder.glob("*.txt"):\n    f.rename(f.with_suffix(".md"))', note: "The same loop can batch-rename a whole folder of files.", runes: ["f", ".rename", "(", "new", ")"] },
    { ctx: "Collect", code: 'rows = [process(f) for f in files]', note: "A comprehension gathers a result from every file at once.", runes: ["[", "process(f)", "for", "f", "in", "files", "]"] },
  ],
  debuggite: [
    { ctx: "Log", code: 'logging.info("data loaded")', note: "A log line records what happened.", runes: ["logging", ".info", "(", "msg", ")"] },
    { ctx: "Pause", code: 'breakpoint()', note: "Drop this in and the program pauses so you can inspect values.", runes: ["breakpoint", "(", ")"] },
    { ctx: "Time", code: 'import timeit\ntimeit.timeit(load, number=100)', note: "timeit measures how long a piece of code takes to run.", runes: ["timeit", ".timeit", "(", "load", ")"] },
  ],
  asyncat: [
    { ctx: "Define", code: 'async def fetch(u):\n    return await client.get(u)', note: "async marks a function that can pause and await slow work.", runes: ["async", "def", "fetch(u)", ":"] },
    { ctx: "Await", code: 'data = await fetch(url)', note: "await lets other tasks run while this one waits for a reply.", runes: ["data", "=", "await", "fetch(url)"] },
    { ctx: "Run", code: 'asyncio.run(main())', note: "asyncio.run starts the whole async program from the top.", runes: ["asyncio", ".run", "(", "main()", ")"] },
  ],
};

export const TIERS = {
  1: { name: "Import", why: "Imports first. Summon your tools before you use them." },
  2: { name: "Setup", why: "Then setup. Define values, functions, and shapes before you use them." },
  3: { name: "Data", why: "Then data. Load it in before you can work with it." },
  4: { name: "Logic", why: "Then logic. Loops, conditions, and transforms do the work." },
  5: { name: "Reveal", why: "Reveal last. Charts and files come after the work is done." },
  6: { name: "Prove", why: "Prove it. Tests verify the finished working." },
};

export const CARDS = [
  { id: "tomes", name: "Import", real: "brings a library in", school: "transmutation", base: 6, tier: 1, code: "import pandas as pd", mnem: "Pull a tool in from outside." },
  { id: "vessel", name: "Variable", real: "names a value", school: "inscription", base: 5, tier: 2, code: "goal = 100", mnem: "A jar holding a value." },
  { id: "reliquary", name: "Collection", real: "list / dict / set", school: "inscription", base: 6, tier: 2, code: 'orders = [{"drink": "chai", "sales": 30}]', mnem: "A shelf of items." },
  { id: "bound", name: "Function", real: "reusable block", school: "inscription", base: 7, tier: 2, code: 'def total(rows): return sum(r["sales"] for r in rows)', mnem: "Input in, result out." },
  { id: "blueprint", req: "classalisk", name: "Class", real: "blueprint for objects", school: "transmutation", base: 8, tier: 2, code: 'class Shop: name = "Bettany"', mnem: "One sheet, forty stamped copies." },
  { id: "comet", req: "fastfoxx", name: "Read File", real: "reading a file", school: "divination", base: 9, tier: 3, code: 'df = pd.read_csv("orders.csv")', mnem: "Data arriving from afar." },
  { id: "ledger", name: "pandas", real: "tables and dataframes", school: "divination", base: 8, tier: 3, code: "df = pd.DataFrame(orders)", mnem: "Rows and columns." },
  { id: "call", req: "requestro", name: "API Call", real: "API request", school: "artifice", base: 7, tier: 3, code: "resp = requests.get(url)", mnem: "Send out, get an answer back." },
  { id: "branch", name: "Loop", real: "if / for / while", school: "inscription", base: 6, tier: 4, code: 'for r in orders: print(r["drink"])', mnem: "A fork you choose." },
  { id: "weaving", req: "comprehendra", name: "Comprehension", real: "builds a list in one line", school: "inscription", base: 7, tier: 4, code: 'names = [r["drink"] for r in orders]', mnem: "A loop folded into brackets." },
  { id: "stream", req: "genryu", name: "Generator", real: "yields one at a time", school: "transmutation", base: 7, tier: 4, code: 'gen = (r["sales"] for r in orders)', mnem: "A stream, one drop at a time." },
  { id: "ward", req: "pandasaur", name: "Fill Missing", real: "handling missing data", school: "inscription", base: 6, tier: 4, code: "df = df.fillna(0)", mnem: "Fill the gaps so nothing breaks." },
  { id: "glass", req: "plotlynx", name: "Chart", real: "Plotly chart", school: "divination", base: 8, tier: 5, code: 'fig = px.bar(df, x="drink", y="sales")', mnem: "A chart you can read." },
  { id: "threshold", req: "pathio", name: "Open File", real: "file I/O", school: "transmutation", base: 7, tier: 5, code: 'df.to_csv("report.csv")', mnem: "A doorway for data." },
  { id: "familiar", req: "autogolem", name: "Schedule", real: "automation", school: "artifice", base: 8, tier: 5, code: "schedule.every().day.do(backup)", mnem: "A wheel that repeats the work." },
  { id: "proving", req: "pytestrix", name: "Test", real: "pytest", school: "artifice", base: 7, tier: 6, code: "assert len(df) >= 0", mnem: "A check. It passes." },
  { id: "runeimport", name: "Import Tool", real: "import (a 2nd tool)", school: "divination", base: 6, tier: 1, code: "import plotly.express as px", mnem: "Bring a second tool into the circle." },
  { id: "sieve", req: "pandasaur", name: "Filter", real: "filtering rows", school: "divination", base: 8, tier: 3, code: "df = df[df.sales > 20]", mnem: "Keep only the rows you need." },
  { id: "mapweave", req: "pandasaur", name: "Apply", real: "apply / map", school: "transmutation", base: 7, tier: 4, code: 'df["big"] = df.sales > 20', mnem: "One change, across every row." },
  { id: "printword", name: "Print", real: "shows a value", school: "inscription", base: 6, tier: 5, code: "print(df)", mnem: "Speak the result aloud." },
  { id: "seer", req: "debuggite", name: "Breakpoint", real: "debugging", school: "artifice", base: 7, tier: 6, code: "breakpoint()", mnem: "Pause and inspect the script." },
  { id: "gitbranch", req: "inherita", name: "Inheritance", real: "subclass of another class", school: "transmutation", base: 10, tier: 6, code: "class Puppy(Dog): pass", mnem: "Build on what came before." },
  { id: "sealed", req: "dunder", name: "Magic Method", real: "magic methods", school: "transmutation", base: 10, tier: 6, code: "def __eq__(self, o): return self.x == o.x", mnem: "Teach your object the language's tongue." },
  { id: "typed", req: "typeward", name: "Type Hint", real: "type hints", school: "inscription", base: 8, tier: 5, code: "def f(x: int) -> str: return str(x)", mnem: "Say what goes in and comes out." },
  { id: "decorator", req: "propward", name: "Property", real: "getter that looks like a field", school: "transmutation", base: 9, tier: 5, code: "value = property(getter)", mnem: "A method that reads like a field." },
  { id: "cells", req: "vaultdb", name: "SQL Query", real: "SQL / sqlite3", school: "divination", base: 10, tier: 6, code: 'con.execute("SELECT * FROM teas")', mnem: "Records that outlive the run." },
  { id: "window", req: "chronos", name: "Datetime", real: "dates and durations", school: "inscription", base: 8, tier: 5, code: "due = start + timedelta(days=7)", mnem: "A moment, and the gap between." },
];

export const ENEMIES = [
  { id: "wraith", art: "wraith", name: "The Unclosed", weak: "inscription", hp: 95,
    quip: "Every colon you forgot, wearing a shape.",
    moves: [{ t: "atk", v: 5 }, { t: "atk", v: 5 }, { t: "atk", v: 6 }] },
  { id: "specter", art: "specter", name: "The Null", weak: "transmutation", hp: 145,
    quip: "It goes thin when struck. Land one long working, in order.",
    moves: [{ t: "atk", v: 5 }, { t: "guard" }, { t: "atk", v: 7 }] },
  { id: "oracle", art: "oracle", name: "The Overfit", weak: "divination", hp: 200,
    quip: "It knows last week by heart and nothing else. Outrun it with a clean pipeline.",
    moves: [{ t: "atk", v: 6 }, { t: "heal", v: 10 }, { t: "atk", v: 7 }] },
  { id: "segfault", art: "segfault", name: "The Segfault", weak: "artifice", hp: 275, boss: true,
    quip: "Whatever got into the book underneath everything else. It hits hard and mends deeper.",
    moves: [{ t: "atk", v: 8 }, { t: "heal", v: 12 }, { t: "atk", v: 10 }] },
];

// paired by kind with the story inks: structure, care, risk, number
export const WORK_SCHOOL = {
  deepcraft: { name: "Deep Craft", color: SCHOOL_INK.tide, blurb: "Inheritance, and the methods that plug your classes into the language." },
  precision: { name: "Precision", color: SCHOOL_INK.verte, blurb: "Types and tests that catch the lie before it runs." },
  vault: { name: "The Vault", color: SCHOOL_INK.oxblood, blurb: "Where a program remembers things after it closes." },
  numeric: { name: "The Numeric Arts", color: SCHOOL_INK.folium, blurb: "The arrays and models machine learning runs on." },
  deeproots: { name: "The Deep Roots", color: SCHOOL_INK.tide, blurb: "The algorithms and structures under every program." },
  newtongue: { name: "The New Tongue", color: SCHOOL_INK.verte, blurb: "Newer syntax that reads like someone meant it." },
  swift: { name: "The Swift Arts", color: SCHOOL_INK.oxblood, blurb: "Doing more than one thing at once, and finding what is slow." },
  measured: { name: "The Measured Arts", color: SCHOOL_INK.folium, blurb: "Numerical methods and signal tools." }
};

export const WORK = [
  { id: "inherita", from: "classalisk", name: "Inheritance", school: "deepcraft", rank: 3,
    real: "Inheritance & super()", desc: "Build a class on top of another. The child gets the parent's methods for free, can override any of them, and calls super() to run the parent's version before adding its own. This is how frameworks let you extend their machinery.",
    say: "A subclass IS-A parent, plus extra; super() runs the parent's version first.", image: "A copy of the book inherits the original's whole spellbook. She can use any page as-is, rewrite a page in her own hand (override), or add to the teacher's words rather than erase them (super()).", when: "Whenever two classes share behavior, or a library says 'subclass this base class'.",
    code: 'class Dog(Animal):\n    def speak(self):\n        return f"{self.name} says woof"', runes: ["class", "Dog(Animal)", ":", "def speak(self)", ":"],
    mission: "Make a base class and a subclass that overrides one method and calls super().__init__ in its own __init__.", gotcha: "A subclass shares the parent's class-level attributes, and forgetting super().__init__() means the parent's setup never runs. Your object ends up half-built." },
  { id: "dunder", from: "classalisk", name: "Magic Methods", school: "deepcraft", rank: 4,
    real: "Magic methods (__eq__, __lt__ ...)", desc: "The double-underscore methods let YOUR objects behave like built-ins. __repr__ decides how they print, __eq__ how == compares them, __lt__ how they sort, __len__ what len() returns. Define them and Python's operators just work on your types.",
    say: "Dunder methods wire your class into Python's built-in operators.", image: "You carve slots into a blank shape so the world's spells fit it: an == slot, a < slot, a len() slot. Once carved, every standard incantation works on it as if it were born knowing them.", when: "Whenever you want ==, <, sorting, len(), or clean printing to work on your own class.",
    code: 'class Money:\n    def __eq__(self, other):\n        return self.cents == other.cents\n    def __lt__(self, other):\n        return self.cents < other.cents', runes: ["def __eq__(self, other)", ":", "return", "self.cents == other.cents"],
    mission: "Give a class __repr__ and __eq__ so two instances with the same data print nicely and compare equal.", gotcha: "Define __eq__ and you usually must define __hash__ too, or your objects cannot go in a set or dict key. And __eq__ should handle a different type instead of crashing." },
  { id: "typeward", from: "functo", name: "Type Hints", school: "precision", rank: 3,
    real: "Type hints & mypy", desc: "Annotations that say what a function expects and returns. Python does not enforce them at runtime, but tools like mypy read them to catch whole classes of bugs before you run. Passing a str where an int belongs, returning None by accident. Optional and | mark 'might be missing'.",
    say: "Type hints are a contract mypy checks, not a runtime rule.", image: "Every doorway in the workshop wears a stencil: ints pass here, strings there. Nothing is physically blocked, but the inspector (mypy) walks the halls beforehand and flags anyone in the wrong doorway.", when: "On any function others will call, and any codebase big enough to lose track of what returns what.",
    code: 'def greet(name: str, times: int = 1) -> str:\n    return (f"hi {name} " * times).strip()', runes: ["def greet(", "name: str,", "times: int = 1", ") -> str", ":"],
    mission: "Annotate a function's parameters and return type, then picture what mypy would say if you returned the wrong type.", gotcha: "Hints are not enforced when you run. Greet(5) still runs and may crash inside. Their value only appears when you actually run mypy (or your editor) over the code." },
  { id: "propward", from: "classalisk", name: "Properties", school: "precision", rank: 3,
    real: "Properties (@property)", desc: "A method that acts like an attribute. @property lets obj.area read like a plain field while running code behind it. Computed on the fly, validated on set. It is how you add logic to an attribute without changing how callers use it.",
    say: "@property turns a method into a read-like attribute. No parentheses at the call site.", image: "A dial on a cabinet labelled temperature. Turning it feels like touching a simple attribute, but inside, gears (your code) compute and check the value each time it is read or turned.", when: "When an attribute should be computed from others, or a set should be validated, without callers writing obj.get_x().",
    code: 'class Circle:\n    @property\n    def area(self):\n        return 3.14159 * self.r ** 2', runes: ["@property", "def area(self)", ":", "return", "3.14159 * self.r ** 2"],
    mission: "Add a computed @property to a class and read it with no parentheses, like obj.area.", gotcha: "You read a property with no parentheses: obj.area, not obj.area(). And a bare @property is read-only. Assigning obj.area = 5 raises unless you also write an @area.setter." },
  { id: "vaultdb", from: "pathio", name: "SQLite", school: "vault", rank: 4,
    real: "Databases (sqlite3 & SQL)", desc: "How programs remember data between runs. sqlite3 ships with Python. Connect to a file, run SQL to create tables and query rows, and always pass values as parameters rather than gluing them into the string. SELECT reads, INSERT writes, WHERE filters.",
    say: "connect, then execute SQL with ? placeholders, then commit.", image: "A vast archive of labelled drawers. SQL is the request slip you hand the archivist: give me every row WHERE price under ten. The ? placeholders are sealed slots so no forged slip (injection) sneaks past.", when: "Any time data must outlive the program. Users, orders, records, anything you would otherwise lose on exit.",
    code: 'import sqlite3\ncon = sqlite3.connect("shop.db")\ncon.execute("INSERT INTO teas VALUES (?, ?)", ("chai", 4))\ncon.commit()', runes: ["con.execute(", "\"INSERT INTO teas VALUES (?, ?)\",", "(\"chai\", 4)", ")"],
    mission: "Open a sqlite3 database, create a table, insert one row with ? parameters, and commit.", gotcha: "Never build SQL by string-formatting user input. Use ? placeholders, or you invite SQL injection. And changes are not saved until you call commit()." },
  { id: "chronos", from: "varibuddy", name: "Dates and Times", school: "vault", rank: 3,
    real: "Dates, times & durations", desc: "Real data is full of dates, and they bite. datetime is a moment, timedelta a span between moments; strptime reads text into a datetime and strftime writes it back out. Store times in UTC, show them local, and dates stop being a source of pain.",
    say: "datetime is a moment; timedelta is the gap between two moments.", image: "A great brass clock in the guild hall. Its hands mark a datetime; the arc you sweep them through is a timedelta; the plate beneath renders the reading in any script you like (strftime).", when: "Whenever you parse a date from a file, measure how long something took, or work out a deadline.",
    code: 'from datetime import datetime, timedelta\nstart = datetime(2025, 3, 1)\ndue = start + timedelta(days=14)\nprint(due.strftime("%b %d"))', runes: ["due", "=", "start", "+", "timedelta(days=14)"],
    mission: "Parse a date string with strptime, add a timedelta, and format the result with strftime.", gotcha: "A naive datetime has no time zone, so comparing one machine's now to another's can be silently wrong. Prefer UTC (datetime.now(timezone.utc)) and convert to local only for display." },
  { id: "vectorcraft", from: "numpyra", name: "Vectorization", school: "numeric", rank: 4,
    real: "NumPy vectorization & broadcasting", desc: "The core habit of numerical Python: act on a whole array at once instead of looping. NumPy applies +, *, and functions element-wise across millions of numbers in a single line, and broadcasting stretches a smaller array to fit a larger one. This is why fast ML code almost never has a for-loop over numbers.",
    say: "Never loop over an array. Vectorize the whole thing at once.", image: "A room of scribes all ruling the same line at the same moment on one word from the front. Broadcasting is what happens when one of them has a shorter page and the room quietly stretches it to match.", when: "Any time you catch yourself about to write a for-loop over numbers. There is almost always a vectorized way that is faster and clearer.",
    code: 'import numpy as np\narr = np.array([1, 2, 3, 4])\nscaled = arr * 10 + 1', runes: ["import numpy as np", "arr = np.array([1, 2, 3, 4])", "scaled = arr * 10 + 1"],
    mission: "Turn a Python list of numbers into a NumPy array and transform every element with one vectorized expression. No loop.", gotcha: "A NumPy array has one fixed dtype. Mixing ints and strings silently makes everything strings, and an int array truncates a float you assign into it. Broadcasting also fails loudly when shapes are incompatible." },
  { id: "framewright", from: "pandasaur", name: "groupby and merge", school: "numeric", rank: 4,
    real: "pandas: groupby, merge & reshape", desc: "Real data work is reshaping and combining tables, and this is most of a data scientist's day. groupby splits rows into groups and aggregates each; merge joins two DataFrames on a shared key, exactly like a database join; pivot reshapes long data into wide.",
    say: "split-apply-combine: groupby splits, you aggregate, pandas recombines.", image: "A great ledger you can fold and refile at will. Gather every row by category and total each (groupby), or bind two ledgers along a shared column (merge).", when: "Whenever you need a summary per category, or need to pull information from two tables into one.",
    code: 'summary = df.groupby("drink")["sales"].sum()\njoined = df.merge(prices, on="drink", how="left")', runes: ["df", ".groupby(\"drink\")", "[\"sales\"]", ".sum()"],
    mission: "Group a DataFrame by a category column and compute the mean or sum per group, then merge in a second table on a shared key.", gotcha: "merge defaults to an inner join, so rows with no match in either table silently vanish. Pass how='left' to keep every left row. And groupby returns a new object; it never changes the original df in place." },
  { id: "learnedmachine", from: "pandasaur", name: "scikit-learn", school: "numeric", rank: 5,
    real: "scikit-learn (fit / predict)", desc: "The gateway to machine learning. scikit-learn gives every model the same shape: create it, call .fit(X, y) to learn from labelled data, then .predict(X_new) on data it has never seen. Classification, regression, clustering, all of it follows that one fit/predict shape.",
    say: "Every sklearn model is the same shape: make it, fit it, predict with it.", image: "A blank machine you show many labelled examples (fit) until it can name a new thing on its own (predict). Every golem in the workshop learns the very same way.", when: "Whenever you want a program to learn a pattern from examples. Predicting a number, sorting into categories, or grouping the unlabelled.",
    code: 'from sklearn.linear_model import LinearRegression\nmodel = LinearRegression()\nmodel.fit(X_train, y_train)\npreds = model.predict(X_test)', runes: ["model = LinearRegression()", "model.fit(X_train, y_train)", "preds = model.predict(X_test)"],
    mission: "Import a scikit-learn model, fit it on training features and labels, then predict on a held-out test set.", gotcha: "Never fit on your test data. That leaks the answers and gives a falsely perfect score. Fit only on the training split, and X must be 2D (a table of features) even for a single feature (reshape(-1, 1))." },
  { id: "assayer", from: "pandasaur", name: "Model Evaluation", school: "numeric", rank: 5,
    real: "Model evaluation & train/test split", desc: "A model that memorizes its training data but fails on new data is worthless. This is overfitting, the central danger in ML. train_test_split holds back data the model never sees, so you can measure honest performance with a metric like accuracy, precision, or mean squared error.",
    say: "Split off a test set the model never sees, then score it honestly.", image: "A proving where the thing being tested never grades its own homework. You hold back sealed questions (the test set) and judge only on those.", when: "Every single time you train a model. Before you trust any score at all, split the data and evaluate on the part held back.",
    code: 'from sklearn.model_selection import train_test_split\nX_tr, X_te, y_tr, y_te = train_test_split(X, y, test_size=0.2)\nscore = model.fit(X_tr, y_tr).score(X_te, y_te)', runes: ["X_tr, X_te, y_tr, y_te =", "train_test_split(", "X, y,", "test_size=0.2)"],
    mission: "Split your data into train and test sets, fit on train, and report the score on the test set. Then watch whether the train score is much higher (overfitting).", gotcha: "A high training score means nothing alone. If train accuracy is 0.99 but test is 0.60, the model overfit. And accuracy misleads on imbalanced data (99% 'no' means a model that always says 'no' scores 99%); reach for precision and recall there." },
  { id: "deeploom", from: "numpyra", name: "PyTorch", school: "numeric", rank: 6,
    real: "PyTorch tensors & autograd", desc: "The foundation of deep learning. A tensor is NumPy's array with two new powers: it can run on a GPU, and it remembers every operation done to it so gradients can flow backward (autograd). A neural network is really just tensors, layers, and gradient descent. And this is where that begins.",
    say: "A tensor is a NumPy array that remembers its own gradient.", image: "A loom whose every thread remembers how it was pulled, so you can trace any finished pattern back to the tug that shaped it. That backward trace is exactly how a network learns.", when: "When you move from classical ML into deep learning. Neural networks, embeddings, anything trained by gradient descent.",
    code: 'import torch\nx = torch.tensor([2.0], requires_grad=True)\ny = x ** 2\ny.backward()', runes: ["x = torch.tensor([2.0], requires_grad=True)", "y = x ** 2", "y.backward()"],
    mission: "Make a PyTorch tensor with requires_grad, run an operation on it, call backward(), and read the gradient in .grad.", gotcha: "Gradients accumulate. You must zero them each training step (optimizer.zero_grad()) or they pile up across batches. And you cannot call .numpy() on a tensor that tracks grad; detach it first with .detach()." },
  { id: "chronicle", from: "functo", name: "Logging", school: "precision", rank: 3,
    real: "Logging (the grown-up print)", desc: "In real code you do not debug with print. You log. The logging module records what happened with severity levels (debug, info, warning, error), timestamps, and the power to silence or redirect output without touching your code. It is how you understand a long program that ran without you watching.",
    say: "Replace print with logging. Levels, timestamps, and an off switch.", image: "A tireless scribe noting every event in the margin with a mark of importance, so that later you can read only the warnings, or the whole tale, without changing what happened.", when: "Any program that runs longer than a quick script. Training runs, servers, automations. Anywhere you would otherwise scatter print statements.",
    code: 'import logging\nlogging.basicConfig(level=logging.INFO)\nlogging.info("epoch %d done", n)\nlogging.warning("loss is climbing")', runes: ["import logging", "logging.basicConfig(level=logging.INFO)", "logging.info(\"epoch %d done\", n)"],
    mission: "Set up logging, replace a print with logging.info, and add a logging.warning for something that might be going wrong.", gotcha: "Call basicConfig once, before any logging happens, or it is quietly ignored. And logging.debug messages are hidden until you lower the level. The silence is the default filter, not an error." },
  { id: "thetrial", from: "pytestrix", name: "Fixtures and Mocking", school: "precision", rank: 4,
    real: "pytest depth: fixtures, mocking, parametrize", desc: "Real test suites go well past bare asserts. A fixture provides reusable setup (a database, sample data) to many tests; parametrize runs one test across many inputs; mocking swaps something slow or external (an API, a file) for a fake so tests stay fast and isolated. This is how professionals actually test.",
    say: "Fixtures set up, parametrize multiplies, mocks stand in for the outside world.", image: "A proving-hall with prepared stations (fixtures), one trial repeated down a rack of samples (parametrize), and stand-ins for anything too costly to summon for real (mocks).", when: "Any test suite past a handful of asserts. Especially when tests share setup, need to cover many cases, or touch something external.",
    code: 'import pytest\n@pytest.mark.parametrize("n,out", [(2, 4), (3, 9)])\ndef test_square(n, out):\n    assert square(n) == out', runes: ["@pytest.mark.parametrize(\"n,out\", [(2, 4), (3, 9)])", "def test_square(n, out):", "assert square(n) == out"],
    mission: "Write one parametrized test that checks a function against several input/output pairs, and add a fixture that supplies shared setup.", gotcha: "A fixture only runs for tests that name it as an argument. Forget the parameter and the setup silently never happens. And over-mocking tests the mock, not your code; mock only the boundary (I/O, network), never the logic you are actually checking." },
  { id: "ouroboros", from: "functo", name: "Recursion", school: "deeproots", rank: 4,
    real: "Recursion", desc: "A function that calls itself. Every recursion needs a base case that stops it and a recursive case that shrinks the problem toward that base. It is the natural way to walk trees, explore possibilities, and solve problems that contain smaller copies of themselves.",
    say: "Solve the tiny case directly; for anything bigger, call yourself on a smaller piece.", image: "A serpent eating its own tail, each bite a little smaller, until nothing is left to eat. That final emptiness is the base case.", when: "Anything shaped like itself: folder trees, nested data, factorials, or splitting a problem in half again and again.",
    code: 'def factorial(n):\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)', runes: ["def factorial(n):", "if n <= 1:", "return 1", "return n * factorial(n - 1)"],
    mission: "Write a recursive function with a clear base case and a recursive call that moves toward it. Factorial, or summing a list.", gotcha: "Forget the base case (or never shrink toward it) and Python hits its recursion limit and crashes with RecursionError. Every branch must eventually reach the base." },
  { id: "weighing", from: "functo", name: "Big-O", school: "deeproots", rank: 4,
    real: "Big-O & complexity", desc: "How an algorithm's cost grows as the input grows, measured in shape rather than seconds. O(n) doubles the work when data doubles; O(n^2) quadruples it; O(log n) barely moves. This is how you know a solution still holds at scale, and it is the language every technical interview speaks.",
    say: "Big-O measures how the work grows as the pile does.", image: "Scales that ignore the load completely and read only how much further they tip each time you add one more stone. Two scripts can weigh the same today and tip very differently at a thousand.", when: "Choosing between two approaches, or asking whether something will still be fast with a million rows. And in every coding interview you will ever take.",
    code: '# O(n): one pass\ntotal = 0\nfor x in data:\n    total += x', runes: ["total = 0", "for x in data:", "total += x"],
    mission: "Take a slow O(n^2) nested-loop solution and make it faster. Often a dict or set lookup turns O(n^2) into O(n).", gotcha: "Big-O ignores constants and small inputs. An O(n^2) solution can beat O(n log n) on tiny data. It describes growth, not a promise; profile when the real numbers matter." },
  { id: "vessels", from: "listling", name: "Stacks and Queues", school: "deeproots", rank: 4,
    real: "Stacks & queues", desc: "Two ways to hold items in line. A stack is last-in-first-out, like a pile of plates. Append and pop from the end. A queue is first-in-first-out, like a line at a shop. Add at one end, take from the other. They power undo, browser history, breadth-first search, and task scheduling.",
    say: "Stack: last in, first out. Queue: first in, first out.", image: "The spike of receipts by the till, where the only one you can pull is the one you just stabbed on. The queue outside Bettany's runs the other way round and everyone standing in it knows it.", when: "Undo and redo, back buttons, processing tasks in order, or exploring a graph level by level.",
    code: 'from collections import deque\nqueue = deque()\nqueue.append("a")\nfirst = queue.popleft()', runes: ["from collections import deque", "queue = deque()", "queue.append(\"a\")", "first = queue.popleft()"],
    mission: "Use a list as a stack (append / pop) and a deque as a queue (append / popleft), and notice which end each takes from.", gotcha: "Using a plain list as a queue with pop(0) is O(n). It shifts every element each time. Use collections.deque for a real queue; its popleft is O(1)." },
  { id: "branching", from: "classalisk", name: "Trees and Graphs", school: "deeproots", rank: 5,
    real: "Trees & graphs", desc: "Data that points at other data, instead of sitting flat in a row. A tree branches from a root with no loops (file systems, the DOM, decision trees); a graph is nodes joined by edges, loops allowed (maps, social networks, dependencies). You explore them depth-first (dive, then backtrack) or breadth-first (level by level).",
    say: "A tree branches from one root; a graph is nodes joined by edges.", image: "A tree can only be walked away from its root and nothing you pass leads back. A map of villages is not like that at all, which is the entire reason you carry a list of where you have already been.", when: "File systems, family trees, maps and routes, recommendation networks, or any 'what connects to what' problem.",
    code: 'graph = {"a": ["b", "c"], "b": ["d"]}\nfor nb in graph["a"]:\n    visit(nb)', runes: ["graph = {\"a\": [\"b\", \"c\"]}", "for nb in graph[\"a\"]:", "visit(nb)"],
    mission: "Represent a small graph as a dict of node -> neighbors, then walk it depth-first with recursion, tracking which nodes you have already seen.", gotcha: "In a graph with cycles, a naive walk loops forever. Always keep a 'visited' set and skip nodes already seen. Trees have no cycles, so they are safe without one." },
  { id: "sifting", from: "listling", name: "Sorting and Searching", school: "deeproots", rank: 4,
    real: "Sorting & searching", desc: "Ordering data and finding things in it. Python's sorted() and .sort() order anything, and a key= function sorts by a computed value. Once data is sorted, binary search finds an item in O(log n) by halving the range each step. Far faster than scanning. Knowing when each applies is core algorithmic literacy.",
    say: "Sort with a key; once sorted, binary-search by halving.", image: "A sifting screen that settles stones in order, after which you find any stone by splitting the pile in half, then half again.", when: "Ranking results, ordering by a field, de-duplicating, or finding an item fast in a large sorted collection.",
    code: 'people.sort(key=lambda p: p["age"])\nnames = sorted(words, reverse=True)', runes: ["people.sort(", "key=lambda p: p[\"age\"]", ")"],
    mission: "Sort a list of dicts by a key of your choice, then use the bisect module to find where a value belongs in a sorted list.", gotcha: ".sort() changes the list in place and returns None. Writing xs = xs.sort() destroys your data. Use sorted(xs) when you want a new list back and to keep the original." },
  { id: "seercanvas", from: "plotwing", name: "Seaborn", school: "numeric", rank: 4,
    real: "Seaborn & statistical viz", desc: "Seaborn makes statistical plots that matplotlib would make you build by hand. One line gives a distribution, a correlation heatmap, or a category comparison with confidence bands. It's how you look at your data before you model it, and how you catch the things a summary number will never show you.",
    say: "Plot the data before you model it. Seaborn shows the shape in one line.", image: "A scrying canvas that paints the whole spread of your data at once. Its clusters, its outliers, its lean. So you see what the averages hide.", when: "Exploratory analysis. The first thing to do with any new dataset, before choosing a model, to understand distributions and relationships.",
    code: 'import seaborn as sns\nsns.histplot(df["age"])\nsns.heatmap(df.corr())', runes: ["import seaborn as sns", "sns.histplot(df[\"age\"])", "sns.heatmap(df.corr())"],
    mission: "Load a DataFrame and make a seaborn histogram of one column and a correlation heatmap of the numeric columns. Then read what they tell you.", gotcha: "A correlation heatmap only measures linear relationships. Two columns can be strongly related in a curve yet show near-zero correlation. Eyeball a scatter plot too; never trust one number." },
  { id: "refiner", from: "pandasaur", name: "Feature Engineering", school: "numeric", rank: 5,
    real: "Feature engineering & preprocessing", desc: "Raw data almost never goes straight into a model. Feature engineering shapes it: scaling numbers to one range, encoding categories as numbers (one-hot), handling missing values, and building new columns that carry signal. A Pipeline chains these steps so the same transforms apply to train and new data alike.",
    say: "Models learn from features. Building the features is most of the job.", image: "A refiner's bench where raw ore is crushed, sorted, and purified into ingots a golem can actually use. The same recipe applied to every batch.", when: "Between loading data and fitting a model. Almost always the step that decides whether the model is any good.",
    code: 'from sklearn.preprocessing import StandardScaler\nX = StandardScaler().fit_transform(nums)\ncats = pd.get_dummies(df["drink"])', runes: ["X = StandardScaler()", ".fit_transform(nums)"],
    mission: "Scale your numeric features with StandardScaler and one-hot encode a categorical column, then feed the combined result to a model.", gotcha: "Fit your scaler on the training data only, then apply it to test data. Fitting on everything leaks information from the test set. A Pipeline enforces this; doing it by hand is a common, silent mistake." },
  { id: "forging", from: "numpyra", name: "Training Loops", school: "numeric", rank: 6,
    real: "Training loops & optimizers", desc: "The heart of training a neural net by hand. A loop repeats four steps: forward pass (predict), loss (how wrong), backward pass (gradients), and optimizer step (nudge the weights toward less wrong). An optimizer like SGD or Adam sets how big each nudge is. Every deep model, however large, is this loop run many times.",
    say: "Forward, loss, backward, step. Repeat until the loss stops falling.", image: "A smith striking hot metal: each blow (a step) guided by how far the shape is from true (the loss), the hammer's weight (the optimizer) setting how hard to strike.", when: "Training any neural network from scratch, or understanding what a library does under the hood when it calls .fit().",
    code: 'for epoch in range(100):\n    optimizer.zero_grad()\n    loss = criterion(model(X), y)\n    loss.backward()\n    optimizer.step()', runes: ["optimizer.zero_grad()", "loss = criterion(model(X), y)", "loss.backward()", "optimizer.step()"],
    mission: "Write a training loop: zero the gradients, compute the loss, call backward(), and step the optimizer. Then watch the loss fall across epochs.", gotcha: "The four steps must stay in order, and zero_grad() must come first each pass. Skip it and last step's gradients pile onto this one, quietly breaking training. A loss that will not fall usually means the learning rate is off." },
  { id: "reckoning", from: "pandasaur", name: "Metrics", school: "numeric", rank: 5,
    real: "Metrics: precision, recall & confusion", desc: "Accuracy alone lies, especially on imbalanced data. A confusion matrix shows exactly what a classifier got right and wrong; precision asks 'of what I flagged, how much was right?'; recall asks 'of what was truly there, how much did I catch?'. Choosing the right metric for the stakes is a real ML skill.",
    say: "Accuracy hides its failures. A confusion matrix can't.", image: "A reckoning-table that sorts every judgement into true and false, caught and missed, so you know how often you were right, and exactly how you went wrong the rest of the time.", when: "Evaluating any classifier. Especially when classes are imbalanced or one kind of mistake costs far more than the other.",
    code: 'from sklearn.metrics import classification_report, confusion_matrix\nprint(confusion_matrix(y_true, y_pred))\nprint(classification_report(y_true, y_pred))', runes: ["print(confusion_matrix(", "y_true, y_pred))"],
    mission: "Print a confusion matrix and a classification report for a classifier, and decide whether precision or recall matters more for your problem.", gotcha: "On imbalanced data, 99% accuracy can mean the model just predicts the majority every time and catches none of what you care about. Precision and recall trade off. Pushing one usually lowers the other." },
  { id: "matchrite", from: "condifork", name: "Pattern Matching", school: "newtongue", rank: 4,
    real: "match statements (pattern matching)", desc: "Python's match statement (3.10+) is a smarter switch: it matches a value against structural patterns, unpacking as it goes. It shines on shaped data like tuples and dicts, because it lets you branch on the type and the contents at once instead of building a tower of if and elif.",
    say: "Match a value against shapes, and unpack it in the same breath.", image: "A keeper who sorts arrivals not by a label but by their whole silhouette. A pair, a triple, a sealed box. And opens each to the right room.", when: "Branching on the shape of data. Parsing commands, handling different message types, or walking a structured tree of cases.",
    code: 'match point:\n    case (0, 0):\n        print("origin")\n    case (x, y):\n        print(x, y)', runes: ["match point:", "case (0, 0):", "case (x, y):"],
    mission: "Write a match statement that handles at least two structural cases. For example an empty tuple, a pair, and a catch-all with _.", gotcha: "A bare name in a case is a capture pattern, not a comparison. Case FOO binds FOO to anything instead of matching the constant FOO. Use a dotted name like case Color.RED, or a guard, to match a known value. And _ is the wildcard." },
  { id: "enumsigil", from: "classalisk", name: "Enums", school: "newtongue", rank: 3,
    real: "Enums (named constants)", desc: "An Enum is a fixed set of named constants. Color.RED, Status.ACTIVE. Instead of loose strings or magic numbers scattered through your code. It makes invalid states impossible to typo, groups related values, and reads clearly. You compare members, iterate them, and never fear a mistyped 'activ' again.",
    say: "Replace magic strings and numbers with a fixed, named set.", image: "A guild roster where every rank has one true name carved in stone. No one can invent a rank that is not on the list.", when: "Any time you have a fixed set of options you'd otherwise write as loose strings. States, modes, directions, the four suits.",
    code: 'from enum import Enum\nclass Color(Enum):\n    RED = 1\n    BLUE = 2', runes: ["from enum import Enum", "class Color(Enum):", "RED = 1"],
    mission: "Define an Enum for a small fixed set (say the states of a task), then use one of its members in a comparison.", gotcha: "Compare members with is or ==, not by their .value. Color.RED is not the integer 1, even though its value is 1. And two members can't share a value unless you mark one an alias." },
  { id: "walrusward", from: "varibuddy", name: "Walrus Operator", school: "newtongue", rank: 4,
    real: "Walrus operator :=", desc: "The walrus operator := assigns a value and returns it in the same expression, so you can name something right where you test it. It collapses the usual compute it, check it, then use it into one line, which is worth most in a while loop or a comprehension.",
    say: "Assign and test in one move. Name the value right where you check it.", image: "A glance that both sees and remembers in the same instant, so you never have to look twice at the same thing.", when: "When you would otherwise compute a value, assign it, then immediately test it. Reading a stream, filtering in a comprehension, or a while-read loop.",
    code: 'while (line := f.readline()):\n    process(line)', runes: ["while (line := f.readline()):", "process(line)"],
    mission: "Use := to name a value inside a while or if condition so you don't repeat the computation on the next line.", gotcha: "The walrus needs its own parentheses in most spots. If n := len(data) > 10 binds n to the comparison, not to the length. Write if (n := len(data)) > 10. And don't overuse it; a plain assignment above is often clearer." },
  { id: "sealcraft", from: "pathio", name: "JSON and Pickle", school: "newtongue", rank: 4,
    real: "Serialization (JSON & pickle)", desc: "Turning a live object into bytes you can save or send, and back again. json.dump and json.load handle text-friendly data (dicts, lists, numbers) any language can read. Perfect for configs and APIs. pickle freezes almost any Python object exactly, but only Python can thaw it. This is how state survives between runs.",
    say: "Freeze an object to disk with JSON or pickle, then thaw it later.", image: "Sealing a living thing in ice: JSON is clear ice anyone can read through; pickle is a deeper freeze that keeps every detail but only your own kind can melt.", when: "Saving settings or results between runs, sending data over a network, or caching a trained model or computed state.",
    code: 'import json\njson.dump(data, open("out.json", "w"))\nback = json.load(open("out.json"))', runes: ["import json", "json.dump(data, open(\"out.json\", \"w\"))", "back = json.load(open(\"out.json\"))"],
    mission: "Save a dictionary to a JSON file with json.dump, then load it back with json.load and confirm it round-trips.", gotcha: "JSON only handles basic types. A set, a datetime, or a custom class raises TypeError until you convert it first. And never unpickle data you don't trust; a malicious pickle can run arbitrary code when loaded." },
  { id: "pathward", from: "pathio", name: "pathlib", school: "newtongue", rank: 3,
    real: "pathlib (modern file paths)", desc: "pathlib treats file paths as objects, not fragile strings. Path('data') / 'raw' / 'x.csv' builds a path that works on any OS, and a Path knows how to check if it exists, read its own text, list a folder, or find its suffix. It replaces os.path string-juggling with something clean and readable.",
    say: "Build paths with / and let the Path object do the file work.", image: "You say where, and it works out the road, including the fact that this machine spells its roads with a different slash from the last machine you ran on.", when: "Any time you touch the filesystem. Building paths, checking existence, reading files, or walking a directory.",
    code: 'from pathlib import Path\np = Path("data") / "laps.csv"\nif p.exists():\n    text = p.read_text()', runes: ["from pathlib import Path", "p = Path(\"data\") / \"laps.csv\"", "if p.exists():", "text = p.read_text()"],
    mission: "Build a path with the / operator, check whether it exists, and if so read it with .read_text(). No string concatenation.", gotcha: "The / operator only works when at least one side is a Path. Joining two plain strings with / is a TypeError. Start from Path(...). And read_text loads the whole file into memory; for huge files, open and iterate lines instead." },
  { id: "threadweave", from: "asyncat", name: "Threading", school: "swift", rank: 4,
    real: "Threading (I/O concurrency)", desc: "Threads let one program do several things at once. While one waits on a slow download or file, others keep working. Python threads share memory, which makes them light but means you must guard shared data with a Lock. Because of the GIL, threads speed up I/O-bound work (waiting), not CPU-bound crunching.",
    say: "Threads overlap waiting. Great for I/O, not for heavy computation.", image: "Many weavers at one loom, each script a thread while the others wait for dye. But they must take turns at the shared shuttle, or the pattern tangles.", when: "Work that spends time waiting. Downloading many URLs, reading many files, or handling several network calls at once.",
    code: 'import threading\nt = threading.Thread(target=fetch, args=(url,))\nt.start()\nt.join()', runes: ["import threading", "t = threading.Thread(target=fetch, args=(url,))", "t.start()", "t.join()"],
    mission: "Start two threads that each do a waiting task (a sleep or a download), then join them and watch them overlap.", gotcha: "Threads share memory, so two touching the same variable can corrupt it. Wrap shared state in a threading.Lock. And the GIL means threads won't speed up pure computation; reach for multiprocessing there." },
  { id: "sundering", from: "asyncat", name: "Multiprocessing", school: "swift", rank: 5,
    real: "Multiprocessing (CPU parallelism)", desc: "For heavy computation, multiprocessing runs real parallel processes. Each with its own interpreter and memory. Sidestepping the GIL to use every CPU core at once. A Pool maps a function over many inputs across cores. The cost: data must be pickled to cross between processes, so it suits chunky, independent work.",
    say: "Separate processes use every core. The way to actually parallelize CPU work.", image: "One task split among many separate workshops, each fully staffed and independent, finishing in a fraction of the time. Though messages between them travel slowly by courier.", when: "CPU-bound work you can split. Crunching large arrays, running simulations, or applying a heavy function to many items.",
    code: 'from multiprocessing import Pool\nwith Pool(4) as pool:\n    results = pool.map(heavy, items)', runes: ["from multiprocessing import Pool", "with Pool(4) as pool:", "results = pool.map(heavy, items)"],
    mission: "Use a multiprocessing Pool to map a CPU-heavy function over a list across several cores, and compare it to a plain loop.", gotcha: "Everything sent to a worker must be picklable, and passing big data across processes can cost more than it saves. Parallelize chunky work, not tiny fast calls. On Windows and notebooks, guard the launch with an if __name__ check." },
  { id: "flamescry", from: "debuggite", name: "Profiling", school: "swift", rank: 4,
    real: "Profiling & optimization", desc: "Before optimizing, measure. Guessing where a program is slow is almost always wrong. cProfile shows how long each function took and how often it ran, pointing straight at the real bottleneck. timeit measures a small snippet precisely. The rule: profile first, optimize the one hot spot, then measure again.",
    say: "Never guess where the time goes. Profile it, then fix whatever the profile actually points at.", image: "A scrying flame that burns brightest over the part of the script that drinks the most time, so you mend what matters instead of what you assume.", when: "When something is too slow and you need to know why. Before rewriting anything, and again after, to prove it helped.",
    code: 'import cProfile\ncProfile.run("main()")', runes: ["import cProfile", "cProfile.run(\"main()\")"],
    mission: "Profile a slow function with cProfile, read which call eats the most time, and time a small alternative with timeit.", gotcha: "Optimizing before profiling wastes effort on code that was never the bottleneck. The slow part is usually not where you would guess. And micro-optimizing a function called once saves nothing; chase what runs often or long." },
  { id: "scipyrite", from: "numpyra", name: "SciPy", school: "measured", rank: 5,
    real: "SciPy (scientific computing)", desc: "SciPy builds on NumPy with the scientist's toolkit: optimization (find a minimum), integration, interpolation, linear algebra, and a deep stats module. When a problem needs a real numerical method, whether that's fitting a curve or integrating a function, SciPy almost certainly has it already, tested and fast.",
    say: "When you need a real numerical method, SciPy already has it.", image: "A scholar's cabinet of precise instruments. Each drawer a solved problem, from finding the lowest point of a curve to measuring the area beneath it.", when: "Scientific and engineering math beyond NumPy's basics. Curve fitting, optimization, integration, or advanced statistics.",
    code: 'from scipy import optimize\nresult = optimize.minimize(cost, x0)', runes: ["from scipy import optimize", "result = optimize.minimize(cost, x0)"],
    mission: "Use scipy.optimize to find the minimum of a simple function, or scipy.integrate to integrate one. And read the result object.", gotcha: "Many SciPy routines return a rich result object, not a bare number. Optimize.minimize gives an object whose .x holds the answer and .success says whether it converged. Check .success before trusting .x." },
  { id: "statward", from: "pandasaur", name: "Statistics", school: "measured", rank: 5,
    real: "Statistics & hypothesis testing", desc: "The math of drawing conclusions from data under uncertainty. A hypothesis test (like a t-test) asks whether a difference is real or just noise, returning a p-value. The chance you would see this if nothing were going on. Means and standard deviations describe your data. Tests tell you what you're allowed to claim about it.",
    say: "A p-value asks: could this difference be just noise? Below 0.05, probably not.", image: "The claim is treated as nothing at all until the evidence gets embarrassing. The p-value is a measure of how embarrassing, and 0.05 is a line somebody drew in 1925 that we have all been standing on ever since.", when: "Deciding whether a result is real. Comparing two groups, checking if a treatment mattered, or reporting findings in research.",
    code: 'from scipy import stats\nt, p = stats.ttest_ind(group_a, group_b)', runes: ["from scipy import stats", "t, p = stats.ttest_ind(group_a, group_b)"],
    mission: "Run a t-test comparing two groups with scipy.stats.ttest_ind, and interpret the p-value against a 0.05 threshold.", gotcha: "A p-value is not the probability your hypothesis is true, and a small p on a tiny effect can be meaningless. Statistical significance is not practical importance. And p > 0.05 does not prove no effect; it means you did not detect one." },
  { id: "signalward", from: "numpyra", name: "Signal Processing", school: "measured", rank: 5,
    real: "Signal processing", desc: "Extracting meaning from signals that change over time. Exactly the shape of EEG, audio, or sensor data. A Fourier transform (FFT) reveals which frequencies a signal is made of; filters remove noise or isolate a band; convolution smooths. For neuroscience data especially, this is how raw waves become findings.",
    say: "A Fourier transform shows which frequencies hide inside a wave.", image: "A prism for time itself. Feed it a tangled wave and it splits the signal into the pure tones woven within, the way light fans into color.", when: "Any data that varies over time. Brain-wave recordings, audio, vibration, or sensor streams you need to denoise or analyze by frequency.",
    code: 'import numpy as np\nspectrum = np.fft.fft(signal)\nfreqs = np.fft.fftfreq(len(signal))', runes: ["spectrum = np.fft.fft(signal)", "freqs = np.fft.fftfreq(len(signal))"],
    mission: "Take an array that varies over time, run np.fft.fft on it, and find which frequency has the strongest component.", gotcha: "The FFT assumes an evenly sampled signal. Irregular timestamps give meaningless frequencies. And the output is complex; take np.abs for magnitude, and remember the spectrum is mirrored for real inputs (use rfft to skip the redundant half)." },
];

export const TRADE_HANDS = {
  inherita: [
    { ctx: "Override", code: 'class Cat(Animal):\n    def speak(self):\n        return f"{self.name} says meow"', note: "An override replaces the parent method entirely for this class.", runes: ["class", "Cat(Animal)", ":", "def speak(self)", ":"] },
    { ctx: "Extend with super()", code: 'class Puppy(Dog):\n    def __init__(self, name, age):\n        super().__init__(name)\n        self.age = age', note: "super().__init__ runs the parent setup, THEN you add the new part.", runes: ["def __init__(self, name, age)", ":", "super().__init__(name)", "self.age = age"] },
    { ctx: "isinstance", code: 'd = Dog("Rex")\nprint(isinstance(d, Animal))', note: "A Dog is-a Animal, so isinstance is True. Inheritance models 'is a kind of'.", runes: ["print(", "isinstance(", "d, Animal", "))"] },
  ],
  dunder: [
    { ctx: "Printing", code: 'class Point:\n    def __repr__(self):\n        return f"Point({self.x}, {self.y})"', note: "__repr__ is what you see in the console and inside lists. Make it unambiguous.", runes: ["def __repr__(self)", ":", "return", "f\"Point({self.x}, {self.y})\""] },
    { ctx: "Sorting", code: 'coins = sorted([Money(50), Money(10)])', note: "With __lt__ defined, sorted() orders your objects smallest-first.", runes: ["sorted(", "[Money(50), Money(10)]", ")"] },
    { ctx: "len()", code: 'class Deck:\n    def __len__(self):\n        return len(self.cards)', note: "__len__ makes len(deck) work. Return an int.", runes: ["def __len__(self)", ":", "return", "len(self.cards)"] },
  ],
  typeward: [
    { ctx: "Maybe missing", code: 'from typing import Optional\ndef find(key: str) -> Optional[str]:\n    return data.get(key)', note: "Optional[str] means a str or None, .get can return None, so say so.", runes: ["def find(key: str)", "-> Optional[str]", ":", "return", "data.get(key)"] },
    { ctx: "Modern union", code: 'def parse(x: str) -> int | None:\n    return int(x) if x.isdigit() else None', note: "int | None is the newer way to write Optional[int].", runes: ["def parse(x: str)", "-> int | None", ":", "return", "int(x) if x.isdigit() else None"] },
    { ctx: "Collections", code: 'def total(prices: list[float]) -> float:\n    return sum(prices)', note: "list[float] documents what the list holds, element type and all.", runes: ["def total(", "prices: list[float]", ") -> float", ":", "return sum(prices)"] },
  ],
  propward: [
    { ctx: "Computed", code: 'class Box:\n    @property\n    def volume(self):\n        return self.w * self.h * self.d', note: "volume stays in sync with w, h, d. Nothing to update by hand.", runes: ["@property", "def volume(self)", ":", "return", "self.w * self.h * self.d"] },
    { ctx: "Read it plainly", code: 'c = Circle(2)\nprint(c.area)', note: "No parentheses at all. It reads like a field and quietly runs code.", runes: ["print(", "c.area", ")"] },
    { ctx: "Guard on set", code: '@temp.setter\ndef temp(self, value):\n    self._temp = value', note: "A setter guards the value; callers still just write obj.temp = x.", runes: ["@temp.setter", "def temp(self, value)", ":", "self._temp = value"] },
  ],
  vaultdb: [
    { ctx: "Query", code: 'rows = con.execute("SELECT name FROM teas WHERE price < ?", (5,))', note: "The ? is filled from the tuple. Safe, and it handles the quoting for you.", runes: ["con.execute(", "\"SELECT name FROM teas WHERE price < ?\",", "(5,)", ")"] },
    { ctx: "Delete a row", code: 'con.execute("DELETE FROM teas WHERE name = ?", ("chai",))', note: "WHERE with a ? decides which rows the write touches; commit to keep it.", runes: ["con.execute(", "\"DELETE FROM teas WHERE name = ?\",", "(\"chai\",)", ")"] },
    { ctx: "Read all", code: 'for name, price in con.execute("SELECT * FROM teas"):\n    print(name, price)', note: "Each row comes back as a tuple you can unpack right in the for-loop.", runes: ["for name, price in", "con.execute(", "\"SELECT * FROM teas\"", ")", ":"] },
  ],
  chronos: [
    { ctx: "Parse text", code: 'from datetime import datetime\nd = datetime.strptime("2025-03-01", "%Y-%m-%d")', note: "strptime reads text INTO a datetime; the format must match the text exactly.", runes: ["d", "=", "datetime.strptime(", "\"2025-03-01\",", "\"%Y-%m-%d\"", ")"] },
    { ctx: "Measure a span", code: 'elapsed = datetime.now() - start\nprint(elapsed.days)', note: "Subtract two datetimes and you get a timedelta, .days, .seconds, and so on.", runes: ["elapsed", "=", "datetime.now()", "-", "start"] },
    { ctx: "Format out", code: 'print(due.strftime("%A, %b %d"))', note: "strftime turns a datetime back into text in whatever shape you ask for.", runes: ["print(", "due.strftime(", "\"%A, %b %d\"", "))"] },
  ],
  vectorcraft: [
    { ctx: "Element-wise", code: 'a = np.array([1, 2, 3])\nb = np.array([10, 20, 30])\nprint(a + b)', note: "Two arrays add element by element. No loop, no zip needed.", runes: ["a = np.array([1, 2, 3])", "print(a + b)"] },
    { ctx: "Broadcasting", code: 'm = np.array([[1, 2], [3, 4]])\nprint(m + 100)', note: "The scalar 100 is stretched to every cell. That is broadcasting.", runes: ["m = np.array([[1, 2], [3, 4]])", "print(m + 100)"] },
    { ctx: "Boolean mask", code: 'a = np.array([5, 12, 3, 20])\nprint(a[a > 10])', note: "A comparison makes a mask; indexing with it keeps only the matches.", runes: ["a = np.array([5, 12, 3, 20])", "print(a[a > 10])"] },
  ],
  framewright: [
    { ctx: "Group & mean", code: 'df.groupby("drink")["sales"].mean()', note: "Split rows by drink, then average sales within each group.", runes: ["df", ".groupby(\"drink\")", "[\"sales\"]", ".mean()"] },
    { ctx: "Join", code: 'df.merge(prices, on="drink", how="left")', note: "Join prices onto df by drink, keeping every left row.", runes: ["df", ".merge(prices,", "on=\"drink\",", "how=\"left\")"] },
    { ctx: "Tally", code: 'df["drink"].value_counts()', note: "A fast count of how many rows fall in each category.", runes: ["df", "[\"drink\"]", ".value_counts()"] },
  ],
  learnedmachine: [
    { ctx: "Classify", code: 'from sklearn.tree import DecisionTreeClassifier\nclf = DecisionTreeClassifier()\nclf.fit(X, y)', note: "A classifier learns to sort inputs into labelled categories.", runes: ["clf = DecisionTreeClassifier()", "clf.fit(X, y)"] },
    { ctx: "Predict", code: 'labels = clf.predict(X_new)', note: "Same model, new unseen data. It applies what it learned.", runes: ["labels = clf.predict(X_new)"] },
    { ctx: "Scale first", code: 'from sklearn.preprocessing import StandardScaler\nX = StandardScaler().fit_transform(X)', note: "Many models need the features on the same scale first.", runes: ["X = StandardScaler()", ".fit_transform(X)"] },
  ],
  assayer: [
    { ctx: "Split", code: 'X_tr, X_te, y_tr, y_te = train_test_split(X, y, test_size=0.25, random_state=42)', note: "Hold back 25% as a test set; random_state makes the split repeatable.", runes: ["train_test_split(X, y,", "test_size=0.25,", "random_state=42)"] },
    { ctx: "Score", code: 'from sklearn.metrics import accuracy_score\nprint(accuracy_score(y_te, preds))', note: "Compare predictions against the true held-out labels.", runes: ["accuracy_score(", "y_te, preds)"] },
    { ctx: "Cross-validate", code: 'from sklearn.model_selection import cross_val_score\nscores = cross_val_score(model, X, y, cv=5)', note: "Split five ways and average. A sturdier estimate than one split.", runes: ["cross_val_score(model,", "X, y, cv=5)"] },
  ],
  deeploom: [
    { ctx: "Tensor math", code: 'a = torch.tensor([1.0, 2.0])\nb = torch.tensor([3.0, 4.0])\nprint(a * b)', note: "Tensors do vectorized math just like NumPy arrays.", runes: ["a = torch.tensor([1.0, 2.0])", "print(a * b)"] },
    { ctx: "Autograd", code: 'x = torch.tensor([3.0], requires_grad=True)\n(x ** 2).backward()\nprint(x.grad)', note: "backward() computes the gradient; it lands in x.grad.", runes: ["(x ** 2).backward()", "print(x.grad)"] },
    { ctx: "A layer", code: 'import torch.nn as nn\nlayer = nn.Linear(4, 2)', note: "A neural layer maps 4 inputs to 2 outputs. Learnable weights inside.", runes: ["import torch.nn as nn", "layer = nn.Linear(4, 2)"] },
  ],
  chronicle: [
    { ctx: "Levels", code: 'logging.warning("low memory")\nlogging.error("training failed")', note: "Levels rank importance. You can later show only warning and above.", runes: ["logging.warning(\"low memory\")", "logging.error(\"training failed\")"] },
    { ctx: "Turn it up", code: 'logging.basicConfig(level=logging.DEBUG)', note: "Lower the level to DEBUG and every message appears.", runes: ["logging.basicConfig(", "level=logging.DEBUG)"] },
    { ctx: "Named logger", code: 'log = logging.getLogger(__name__)\nlog.info("started")', note: "One logger per module keeps sources straight in a big project.", runes: ["log = logging.getLogger(__name__)", "log.info(\"started\")"] },
  ],
  thetrial: [
    { ctx: "Fixture", code: '@pytest.fixture\ndef sample():\n    return [1, 2, 3]', note: "A fixture supplies reusable setup; tests receive it by naming it.", runes: ["@pytest.fixture", "def sample():", "return [1, 2, 3]"] },
    { ctx: "Parametrize", code: '@pytest.mark.parametrize("x", [0, 1, -1])\ndef test_abs(x):\n    assert abs(x) >= 0', note: "One test body, run once per value in the list.", runes: ["@pytest.mark.parametrize(\"x\", [0, 1, -1])", "def test_abs(x):"] },
    { ctx: "Mock", code: 'from unittest.mock import patch\nwith patch("mod.fetch") as m:\n    m.return_value = 42', note: "Replace a real call with a fake that returns whatever you choose.", runes: ["with patch(\"mod.fetch\") as m:", "m.return_value = 42"] },
  ],
  ouroboros: [
    { ctx: "Countdown", code: 'def down(n):\n    if n == 0:\n        return\n    print(n)\n    down(n - 1)', note: "Each call prints, then calls itself one lower, until n hits 0.", runes: ["def down(n):", "if n == 0:", "return", "down(n - 1)"] },
    { ctx: "Sum a list", code: 'def total(xs):\n    if not xs:\n        return 0\n    return xs[0] + total(xs[1:])', note: "First element plus the total of the rest. The rest shrinks each call.", runes: ["if not xs:", "return 0", "return xs[0] + total(xs[1:])"] },
    { ctx: "Two base cases", code: 'def fib(n):\n    if n < 2:\n        return n\n    return fib(n-1) + fib(n-2)', note: "Two base cases stop it; two recursive calls branch it.", runes: ["if n < 2:", "return n", "return fib(n-1) + fib(n-2)"] },
  ],
  weighing: [
    { ctx: "O(n) linear", code: 'for x in items:\n    check(x)', note: "Touch each item once. Double the items, double the work.", runes: ["for x in items:", "check(x)"] },
    { ctx: "O(n^2) nested", code: 'for a in items:\n    for b in items:\n        pair(a, b)', note: "A loop inside a loop. Double the items, quadruple the work.", runes: ["for a in items:", "for b in items:", "pair(a, b)"] },
    { ctx: "O(1) lookup", code: 'seen = set(items)\nif x in seen:\n    hit()', note: "A set membership check is constant time, whatever the size.", runes: ["seen = set(items)", "if x in seen:", "hit()"] },
  ],
  vessels: [
    { ctx: "Stack", code: 'stack = []\nstack.append(1)\ntop = stack.pop()', note: "Append pushes on top; pop takes the most recent back off.", runes: ["stack = []", "stack.append(1)", "top = stack.pop()"] },
    { ctx: "Queue", code: 'from collections import deque\nq = deque([1, 2])\nq.popleft()', note: "popleft takes from the front. First in, first out.", runes: ["q = deque([1, 2])", "q.popleft()"] },
    { ctx: "Undo stack", code: 'history.append(state)\nprev = history.pop()', note: "Push each state; pop to step back. That is undo.", runes: ["history.append(state)", "prev = history.pop()"] },
  ],
  branching: [
    { ctx: "Tree as dict", code: 'tree = {"root": ["a", "b"]}\nprint(tree["root"])', note: "Each key is a node; its list is that node's children.", runes: ["tree = {\"root\": [\"a\", \"b\"]}", "print(tree[\"root\"])"] },
    { ctx: "Track visited", code: 'visited = set()\nif node not in visited:\n    visited.add(node)', note: "The visited set is what stops a graph walk from looping.", runes: ["visited = set()", "if node not in visited:", "visited.add(node)"] },
    { ctx: "Depth-first", code: 'def dfs(n):\n    for nb in graph[n]:\n        dfs(nb)', note: "Recursion dives down each branch before backing out.", runes: ["def dfs(n):", "for nb in graph[n]:", "dfs(nb)"] },
  ],
  sifting: [
    { ctx: "Sort by key", code: 'words.sort(key=len)', note: "Order by each word's length instead of alphabetically.", runes: ["words.sort(", "key=len)"] },
    { ctx: "New sorted list", code: 'ranked = sorted(scores, reverse=True)', note: "sorted() returns a fresh list, highest first, original untouched.", runes: ["ranked = sorted(", "scores, reverse=True)"] },
    { ctx: "Binary search", code: 'import bisect\ni = bisect.bisect_left(xs, target)', note: "On sorted data, bisect finds the spot by halving. O(log n).", runes: ["import bisect", "i = bisect.bisect_left(", "xs, target)"] },
  ],
  seercanvas: [
    { ctx: "Distribution", code: 'sns.histplot(df["price"], kde=True)', note: "See the spread and shape of one column, with a smooth curve.", runes: ["sns.histplot(", "df[\"price\"], kde=True)"] },
    { ctx: "Compare groups", code: 'sns.boxplot(x="drink", y="sales", data=df)', note: "A box per category shows medians and outliers side by side.", runes: ["sns.boxplot(", "x=\"drink\", y=\"sales\",", "data=df)"] },
    { ctx: "Correlations", code: 'sns.heatmap(df.corr(), annot=True)', note: "A colored grid of how every numeric column relates to the rest.", runes: ["sns.heatmap(", "df.corr(), annot=True)"] },
  ],
  refiner: [
    { ctx: "Scale", code: 'from sklearn.preprocessing import StandardScaler\nX = StandardScaler().fit_transform(X)', note: "Put every feature on the same scale so none dominates.", runes: ["X = StandardScaler()", ".fit_transform(X)"] },
    { ctx: "One-hot", code: 'dummies = pd.get_dummies(df["color"])', note: "Turn a text category into 0/1 columns a model can read.", runes: ["dummies = pd.get_dummies(", "df[\"color\"])"] },
    { ctx: "Pipeline", code: 'from sklearn.pipeline import make_pipeline\npipe = make_pipeline(StandardScaler(), model)', note: "Chain steps so the same transforms apply everywhere.", runes: ["pipe = make_pipeline(", "StandardScaler(), model)"] },
  ],
  forging: [
    { ctx: "Zero first", code: 'optimizer.zero_grad()', note: "Clear last step's gradients before computing new ones.", runes: ["optimizer.zero_grad()"] },
    { ctx: "Loss + backward", code: 'loss = criterion(pred, y)\nloss.backward()', note: "Measure the error, then flow gradients back through it.", runes: ["loss = criterion(pred, y)", "loss.backward()"] },
    { ctx: "Step", code: 'optimizer.step()', note: "Nudge every weight a little in the direction that lowers loss.", runes: ["optimizer.step()"] },
  ],
  reckoning: [
    { ctx: "Confusion matrix", code: 'confusion_matrix(y_true, y_pred)', note: "A grid of right vs wrong for each class. The full picture.", runes: ["confusion_matrix(", "y_true, y_pred)"] },
    { ctx: "Full report", code: 'print(classification_report(y_true, y_pred))', note: "Precision, recall, and F1 for every class at once.", runes: ["classification_report(", "y_true, y_pred)"] },
    { ctx: "One metric", code: 'from sklearn.metrics import recall_score\nprint(recall_score(y_true, y_pred))', note: "Recall alone: of all real positives, how many you caught.", runes: ["recall_score(", "y_true, y_pred)"] },
  ],
  matchrite: [
    { ctx: "Match a value", code: 'match cmd:\n    case "start":\n        run()\n    case "stop":\n        halt()', note: "Each case tests the value; the first match wins.", runes: ["match cmd:", "case \"start\":", "case \"stop\":"] },
    { ctx: "Unpack a shape", code: 'match point:\n    case (x, y):\n        print(x + y)', note: "The pattern both matches a pair and names its parts.", runes: ["match point:", "case (x, y):", "print(x + y)"] },
    { ctx: "Catch-all", code: 'match n:\n    case 0:\n        z()\n    case _:\n        other()', note: "_ is the wildcard. It matches anything left over.", runes: ["case 0:", "case _:", "other()"] },
  ],
  enumsigil: [
    { ctx: "Define", code: 'from enum import Enum\nclass Dir(Enum):\n    UP = 1\n    DOWN = 2', note: "Each name is a distinct, fixed member of the set.", runes: ["class Dir(Enum):", "UP = 1", "DOWN = 2"] },
    { ctx: "Use it", code: 'if move == Dir.UP:\n    climb()', note: "Reads clearly and can't be a mistyped string.", runes: ["if move == Dir.UP:", "climb()"] },
    { ctx: "Iterate", code: 'for d in Dir:\n    print(d.name)', note: "You can loop over every member of the set.", runes: ["for d in Dir:", "print(d.name)"] },
  ],
  walrusward: [
    { ctx: "In a while", code: 'while (chunk := read(1024)):\n    handle(chunk)', note: "Read and test in one line. Loop ends when read returns empty.", runes: ["while (chunk := read(1024)):", "handle(chunk)"] },
    { ctx: "In an if", code: 'if (m := re.search(p, s)):\n    print(m.group())', note: "Name the match while checking it exists.", runes: ["if (m := re.search(p, s)):", "print(m.group())"] },
    { ctx: "In a comprehension", code: 'ys = [y for x in xs if (y := f(x)) > 0]', note: "Compute f(x) once, name it y, filter and keep it.", runes: ["[y for x in xs", "if (y := f(x)) > 0]"] },
  ],
  sealcraft: [
    { ctx: "Save JSON", code: 'json.dump({"a": 1}, open("f.json", "w"))', note: "Write a dict to a text file any language can read.", runes: ["json.dump(", "{\"a\": 1},", "open(\"f.json\", \"w\"))"] },
    { ctx: "Load JSON", code: 'data = json.load(open("f.json"))', note: "Read it straight back into a Python dict.", runes: ["data = json.load(", "open(\"f.json\"))"] },
    { ctx: "To a string", code: 's = json.dumps(obj)', note: "dumps (with an s) returns a string instead of writing a file.", runes: ["s = json.dumps(obj)"] },
  ],
  pathward: [
    { ctx: "Build a path", code: 'from pathlib import Path\np = Path("logs") / "run.txt"', note: "The / operator joins parts safely on any OS.", runes: ["p = Path(\"logs\") / \"run.txt\""] },
    { ctx: "Check & read", code: 'if p.exists():\n    data = p.read_text()', note: "A Path can test itself and read its own contents.", runes: ["if p.exists():", "data = p.read_text()"] },
    { ctx: "List a folder", code: 'for f in Path(".").glob("*.csv"):\n    print(f.name)', note: "glob finds matching files; each is a Path with a .name.", runes: ["for f in Path(\".\").glob(\"*.csv\"):", "print(f.name)"] },
  ],
  threadweave: [
    { ctx: "Start a thread", code: 'import threading\nt = threading.Thread(target=job)\nt.start()', note: "The thread runs job while the main program continues.", runes: ["t = threading.Thread(target=job)", "t.start()"] },
    { ctx: "Wait for it", code: 't.join()', note: "join blocks until that thread has finished.", runes: ["t.join()"] },
    { ctx: "Guard shared data", code: 'lock = threading.Lock()\nwith lock:\n    counter += 1', note: "The lock ensures only one thread updates at a time.", runes: ["lock = threading.Lock()", "with lock:", "counter += 1"] },
  ],
  sundering: [
    { ctx: "A pool", code: 'from multiprocessing import Pool\nwith Pool() as p:\n    out = p.map(f, data)', note: "map runs f on every item, spread across cores.", runes: ["with Pool() as p:", "out = p.map(f, data)"] },
    { ctx: "Count cores", code: 'import os\nprint(os.cpu_count())', note: "How many workers your machine can run at once.", runes: ["import os", "print(os.cpu_count())"] },
    { ctx: "Guard launch", code: 'if __name__ == "__main__":\n    main()', note: "Required so child processes don't re-run your script.", runes: ["if __name__ == \"__main__\":", "main()"] },
  ],
  flamescry: [
    { ctx: "Profile a run", code: 'import cProfile\ncProfile.run("solve()")', note: "See every function's total time and call count.", runes: ["import cProfile", "cProfile.run(\"solve()\")"] },
    { ctx: "Time a snippet", code: 'import timeit\nt = timeit.timeit("f(5)", globals=globals())', note: "Measure one small piece precisely, averaged over runs.", runes: ["import timeit", "t = timeit.timeit(\"f(5)\",", "globals=globals())"] },
    { ctx: "Sort by time", code: 'cProfile.run("main()", sort="cumtime")', note: "Rank functions by cumulative time to find the hot path.", runes: ["cProfile.run(\"main()\",", "sort=\"cumtime\")"] },
  ],
  scipyrite: [
    { ctx: "Minimize", code: 'from scipy import optimize\nr = optimize.minimize(f, x0=0)', note: "Find the input that makes f smallest, starting from x0.", runes: ["from scipy import optimize", "r = optimize.minimize(f, x0=0)"] },
    { ctx: "Integrate", code: 'from scipy import integrate\narea, _ = integrate.quad(f, 0, 1)', note: "quad computes the area under f from 0 to 1.", runes: ["from scipy import integrate", "area, _ = integrate.quad(f, 0, 1)"] },
    { ctx: "Interpolate", code: 'from scipy.interpolate import interp1d\ng = interp1d(xs, ys)', note: "Build a function that fills in values between data points.", runes: ["g = interp1d(xs, ys)"] },
  ],
  statward: [
    { ctx: "Mean & spread", code: 'import statistics\nm = statistics.mean(xs)\nsd = statistics.stdev(xs)', note: "The center of your data and how spread out it is.", runes: ["m = statistics.mean(xs)", "sd = statistics.stdev(xs)"] },
    { ctx: "t-test", code: 'from scipy import stats\nt, p = stats.ttest_ind(a, b)', note: "Test whether two groups' means truly differ.", runes: ["t, p = stats.ttest_ind(a, b)"] },
    { ctx: "Read the p", code: 'if p < 0.05:\n    print("significant")', note: "Below the threshold, the difference is unlikely to be noise.", runes: ["if p < 0.05:", "print(\"significant\")"] },
  ],
  signalward: [
    { ctx: "Frequencies", code: 'import numpy as np\nspec = np.fft.fft(sig)', note: "Decompose the signal into its frequency components.", runes: ["spec = np.fft.fft(sig)"] },
    { ctx: "Magnitude", code: 'power = np.abs(spec)', note: "The FFT is complex; abs gives each frequency's strength.", runes: ["power = np.abs(spec)"] },
    { ctx: "Filter", code: 'from scipy.signal import medfilt\nclean = medfilt(sig, 5)', note: "A median filter smooths out spiky noise.", runes: ["from scipy.signal import medfilt", "clean = medfilt(sig, 5)"] },
  ],
};
